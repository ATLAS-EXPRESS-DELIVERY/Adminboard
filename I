<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ATLAS EXPRESS SERVICE — Admin</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&family=Caveat:wght@500;600;700&display=swap" rel="stylesheet">
<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
<style>
:root{
  --ink-dark:#0A1F3D;
  --ink-mid:#0D2B52;
  --ink:#1A3F72;
  --sky-accent:#0EA5E9;
  --sky-light:#E0F2FE;
  --white:#FFFFFF;
  --off-white:#F0F7FF;
  --slate:#4A6080;
  --text:#0D1F36;
  --border:#B0CFEA;
  --red:#B23A3A;
  --green:#1AA37A;
  --sidebar-w:240px;
}
*{margin:0;padding:0;box-sizing:border-box;}
body{font-family:'Inter',sans-serif;background:var(--off-white);color:var(--text);line-height:1.5;}
h1,h2,h3,h4{font-family:'Space Grotesk',sans-serif;}
.mono{font-family:'JetBrains Mono',monospace;}
a{color:inherit;}

/* ===== LOGIN ===== */
.login-wrap{min-height:100vh;display:flex;align-items:center;justify-content:center;
  background:linear-gradient(160deg,var(--ink-dark) 0%,var(--ink-mid) 55%,#0B3060 100%);padding:24px;}
.login-card{background:#fff;border-radius:14px;padding:40px;max-width:420px;width:100%;
  box-shadow:0 20px 60px rgba(0,0,0,0.35);}
.login-card .mark{width:52px;height:52px;border-radius:12px;
  background:linear-gradient(135deg,var(--sky-accent),var(--ink));
  display:flex;align-items:center;justify-content:center;
  color:#fff;font-weight:700;font-size:1.1rem;margin-bottom:18px;
  font-family:'Space Grotesk',sans-serif;letter-spacing:-0.03em;}
.login-card h1{font-size:1.5rem;margin-bottom:4px;color:var(--ink-dark);}
.login-card .sub{color:var(--slate);font-size:0.88rem;margin-bottom:28px;}
.field{margin-bottom:18px;}
.field label{display:block;font-size:0.78rem;text-transform:uppercase;letter-spacing:0.1em;
  color:var(--slate);font-weight:600;margin-bottom:8px;}
.field input{width:100%;padding:13px 14px;border-radius:8px;border:1.5px solid var(--border);
  font-size:0.95rem;background:#fff;color:var(--text);}
.field input:focus{outline:none;border-color:var(--sky-accent);box-shadow:0 0 0 3px rgba(14,165,233,0.14);}
.btn{border:none;border-radius:8px;padding:13px 22px;
  font-family:'Space Grotesk',sans-serif;font-weight:600;font-size:0.95rem;cursor:pointer;transition:transform .15s,box-shadow .15s;}
.btn-primary{background:var(--ink);color:#fff;}
.btn-primary:hover{transform:translateY(-1px);box-shadow:0 6px 18px rgba(26,63,114,0.4);background:var(--sky-accent);}
.btn-secondary{background:var(--ink-dark);color:#fff;}
.btn-secondary:hover{background:var(--ink-mid);}
.btn-danger{background:#fff;color:var(--red);border:1.5px solid #E8C7C7;}
.btn-danger:hover{background:#FCEDED;}
.btn-green{background:var(--sky-accent);color:#fff;}
.btn-green:hover{background:var(--ink);}
.btn-outline{background:#fff;color:var(--ink);border:1.5px solid var(--border);}
.btn-outline:hover{border-color:var(--sky-accent);color:var(--sky-accent);}
.login-error{margin-top:14px;font-size:0.85rem;color:var(--red);font-weight:500;display:none;}

/* ===== SHELL / SIDEBAR ===== */
.shell{display:none;min-height:100vh;}
.sidebar{position:fixed;top:0;left:0;bottom:0;width:var(--sidebar-w);
  background:linear-gradient(180deg,var(--ink-dark) 0%,#081830 100%);
  color:#fff;display:flex;flex-direction:column;z-index:50;
  border-right:1px solid rgba(255,255,255,0.08);}
.sidebar-brand{padding:22px 20px;display:flex;align-items:center;gap:11px;
  border-bottom:1px solid rgba(255,255,255,0.08);}
.sidebar-brand .mark{width:38px;height:38px;border-radius:9px;flex-shrink:0;
  background:linear-gradient(135deg,var(--sky-accent),var(--ink));
  display:flex;align-items:center;justify-content:center;color:#fff;font-weight:700;font-size:0.85rem;
  letter-spacing:-0.03em;border:2px solid rgba(255,255,255,0.25);}
.sidebar-brand .name{font-family:'Space Grotesk',sans-serif;font-weight:700;font-size:0.98rem;line-height:1.2;}
.sidebar-brand .tag{font-size:0.68rem;color:rgba(255,255,255,0.5);margin-top:2px;letter-spacing:0.03em;}
.nav-section-label{font-size:0.68rem;text-transform:uppercase;letter-spacing:0.12em;color:rgba(255,255,255,0.35);
  padding:18px 20px 8px;font-weight:600;}
.nav-item{display:flex;align-items:center;gap:12px;padding:12px 20px;font-size:0.9rem;font-weight:500;
  color:rgba(255,255,255,0.68);cursor:pointer;border-left:3px solid transparent;transition:all .15s;}
.nav-item .ic{font-size:1.05rem;width:20px;text-align:center;}
.nav-item:hover{background:rgba(255,255,255,0.05);color:#fff;}
.nav-item.active{background:rgba(14,165,233,0.14);color:#fff;border-left-color:var(--sky-accent);}
.sidebar-footer{margin-top:auto;padding:16px 20px;border-top:1px solid rgba(255,255,255,0.08);}
.sidebar-footer .who{font-size:0.78rem;color:rgba(255,255,255,0.55);margin-bottom:10px;}
.sidebar-footer .who strong{color:#fff;display:block;font-size:0.88rem;margin-top:2px;}
.sidebar-toggle{display:none;}

.content-wrap{margin-left:var(--sidebar-w);min-height:100vh;display:flex;flex-direction:column;}
.topbar{background:#fff;border-bottom:1px solid var(--border);padding:16px 28px;
  display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:10px;}
.topbar h1{font-size:1.25rem;color:var(--ink-dark);}
.topbar .sub{font-size:0.82rem;color:var(--slate);margin-top:2px;}
.topbar-actions{display:flex;gap:10px;align-items:center;}
.menu-btn{display:none;background:var(--ink);color:#fff;border:none;border-radius:8px;width:38px;height:38px;font-size:1.1rem;cursor:pointer;}

main{flex:1;max-width:1300px;width:100%;margin:0 auto;padding:26px 24px 60px;}
.tab-content{display:none;}
.tab-content.active{display:block;}

.grid{display:grid;grid-template-columns:1.1fr 1.4fr;gap:24px;}
@media(max-width:980px){.grid{grid-template-columns:1fr;}}
.panel{background:#fff;border-radius:14px;padding:28px;border:1px solid var(--border);margin-bottom:24px;}
.panel h2{font-size:1.15rem;margin-bottom:4px;color:var(--ink-dark);}
.panel .sub{color:var(--slate);font-size:0.85rem;margin-bottom:22px;}
.section-title{font-size:0.75rem;text-transform:uppercase;letter-spacing:0.1em;
  color:var(--sky-accent);font-weight:700;margin:20px 0 12px;padding-bottom:6px;
  border-bottom:2px solid var(--sky-light);}
.form-row{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-bottom:14px;}
@media(max-width:560px){.form-row{grid-template-columns:1fr;}}
.form-row-3{display:grid;grid-template-columns:1fr 1fr 1fr;gap:14px;margin-bottom:14px;}
@media(max-width:700px){.form-row-3{grid-template-columns:1fr 1fr;}}
@media(max-width:480px){.form-row-3{grid-template-columns:1fr;}}
.field-inline label{display:block;font-size:0.78rem;text-transform:uppercase;letter-spacing:0.1em;
  color:var(--slate);font-weight:600;margin-bottom:8px;}
.field-inline input,.field-inline select,.field-inline textarea{
  width:100%;padding:12px 14px;border-radius:8px;border:1.5px solid var(--border);
  font-size:0.92rem;background:#fff;color:var(--text);font-family:inherit;}
.field-inline textarea{resize:vertical;min-height:80px;}
.field-inline input:focus,.field-inline select:focus,.field-inline textarea:focus{
  outline:none;border-color:var(--sky-accent);box-shadow:0 0 0 3px rgba(14,165,233,0.12);}

/* ===== TRACKING PANEL (post-create) ===== */
.track-panel{margin-top:20px;border-radius:16px;overflow:hidden;border:1.5px solid var(--sky-accent);
  display:none;}
.track-panel.show{display:block;}
.track-panel-head{background:linear-gradient(120deg,var(--ink-dark),var(--ink-mid));color:#fff;padding:22px 26px;
  display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:14px;}
.track-panel-head .label{font-size:0.74rem;text-transform:uppercase;letter-spacing:0.1em;color:rgba(255,255,255,0.6);margin-bottom:4px;}
.track-panel-head .id{font-family:'JetBrains Mono',monospace;font-size:1.35rem;font-weight:700;}
.track-panel-body{background:#fff;padding:22px 26px;display:flex;gap:24px;flex-wrap:wrap;align-items:center;}
.track-link-box{flex:1;min-width:260px;}
.track-link-box .lbl{font-size:0.72rem;text-transform:uppercase;letter-spacing:0.08em;color:var(--slate);font-weight:600;margin-bottom:8px;}
.track-link-row{display:flex;gap:8px;}
.track-link-row input{flex:1;padding:11px 13px;border-radius:8px;border:1.5px solid var(--border);
  font-family:'JetBrains Mono',monospace;font-size:0.82rem;background:var(--off-white);color:var(--ink-dark);}
.track-link-actions{display:flex;gap:8px;margin-top:10px;flex-wrap:wrap;}
.qr-box{text-align:center;flex-shrink:0;}
.qr-box img{width:104px;height:104px;border-radius:10px;border:1.5px solid var(--border);padding:6px;background:#fff;}
.qr-box .cap{font-size:0.7rem;color:var(--slate);margin-top:6px;}

.copy-btn{background:var(--ink);color:#fff;border:none;border-radius:6px;padding:8px 14px;
  font-size:0.8rem;font-weight:600;cursor:pointer;font-family:'Space Grotesk',sans-serif;}
.copy-btn:hover{background:var(--sky-accent);}
.copy-btn.alt{background:var(--sky-accent);}
.copy-btn.ghost{background:#fff;color:var(--ink);border:1.5px solid var(--border);}

.ship-list{display:flex;flex-direction:column;gap:10px;}
.ship-card{border:1px solid var(--border);border-radius:10px;padding:16px;cursor:pointer;transition:border-color .15s,background .15s;}
.ship-card:hover{border-color:var(--ink);background:var(--sky-light);}
.ship-card.active{border-color:var(--sky-accent);background:var(--sky-light);}
.ship-card .top{display:flex;justify-content:space-between;align-items:center;margin-bottom:6px;}
.ship-card .id{font-family:'JetBrains Mono',monospace;font-weight:600;font-size:0.92rem;color:var(--ink-dark);}
.ship-card .route{font-size:0.85rem;color:var(--slate);}
.ship-card-actions{display:flex;gap:6px;margin-top:10px;flex-wrap:wrap;}
.ship-card-actions .mini-btn{border:1.5px solid var(--border);background:#fff;color:var(--ink);
  border-radius:6px;padding:6px 10px;font-size:0.74rem;font-weight:600;cursor:pointer;
  font-family:'Space Grotesk',sans-serif;display:inline-flex;align-items:center;gap:4px;transition:all .15s;}
.ship-card-actions .mini-btn:hover{border-color:var(--sky-accent);color:var(--sky-accent);background:var(--sky-light);}
.ship-card-actions .mini-btn.mini-danger{color:var(--red);border-color:#E8C7C7;}
.ship-card-actions .mini-btn.mini-danger:hover{background:#FCEDED;border-color:var(--red);}
.ship-card-actions .mini-btn.mini-green{color:var(--green);border-color:#B7E3D3;}
.ship-card-actions .mini-btn.mini-green:hover{background:#E7F8F1;border-color:var(--green);}
.status-badge{display:inline-block;padding:4px 11px;border-radius:99px;
  font-size:0.7rem;font-weight:600;letter-spacing:0.04em;text-transform:uppercase;white-space:nowrap;}
.status-badge.delivered{background:rgba(26,163,122,0.15);color:var(--green);}
.status-badge.transit{background:rgba(14,165,233,0.12);color:var(--sky-accent);}
.status-badge.pending{background:rgba(74,96,128,0.12);color:var(--slate);}
.status-badge.customs{background:rgba(180,140,30,0.15);color:#7a5c10;}
.empty-state{text-align:center;padding:48px 20px;color:var(--slate);}
.empty-state .icon{font-size:2.4rem;margin-bottom:12px;}
.detail-row{display:flex;justify-content:space-between;padding:11px 0;border-bottom:1px solid var(--sky-light);font-size:0.9rem;gap:10px;}
.detail-row:last-child{border-bottom:none;}
.detail-row .k{color:var(--slate);flex-shrink:0;}
.detail-row .v{font-weight:600;text-align:right;max-width:60%;color:var(--ink-dark);}
.actions-row{display:flex;gap:10px;margin-top:18px;flex-wrap:wrap;}
.placeholder-note{color:var(--slate);font-size:0.9rem;text-align:center;padding:30px 0;}
.toast{position:fixed;bottom:24px;right:24px;background:var(--ink-dark);color:#fff;
  padding:14px 20px;border-radius:10px;font-size:0.88rem;font-weight:500;
  box-shadow:0 8px 24px rgba(0,0,0,0.25);z-index:9999;max-width:320px;
  transform:translateY(80px);opacity:0;transition:all 0.3s ease;pointer-events:none;}
.toast.show{transform:translateY(0);opacity:1;}
.toast.success{border-left:4px solid var(--green);}
.toast.error{border-left:4px solid var(--red);}
.toast.info{border-left:4px solid var(--ink);}
.pwd-section{max-width:480px;}
.pwd-status{font-size:0.85rem;margin-top:10px;font-weight:500;display:none;}
.pwd-status.ok{color:var(--green);}
.pwd-status.err{color:var(--red);}
.search-bar{width:100%;padding:11px 14px;border-radius:8px;border:1.5px solid var(--border);
  font-size:0.92rem;margin-bottom:16px;font-family:inherit;background:#fff;color:var(--text);}
.search-bar:focus{outline:none;border-color:var(--sky-accent);}
.stats-row{display:grid;grid-template-columns:repeat(4,1fr);gap:16px;margin-bottom:24px;}
@media(max-width:700px){.stats-row{grid-template-columns:1fr 1fr;}}
.stat-card{background:#fff;border-radius:12px;padding:20px;border:1px solid var(--border);text-align:center;
  border-top:3px solid var(--sky-accent);}
.stat-card.green{border-top-color:var(--green);}
.stat-card.slate{border-top-color:var(--slate);}
.stat-card.amber{border-top-color:#C99A2E;}
.stat-card .val{font-family:'Space Grotesk',sans-serif;font-size:2rem;font-weight:700;color:var(--ink);}
.stat-card .lbl{font-size:0.78rem;color:var(--slate);text-transform:uppercase;letter-spacing:0.06em;margin-top:4px;}
.spinner{display:inline-block;width:16px;height:16px;border:2px solid rgba(255,255,255,0.3);
  border-top-color:#fff;border-radius:50%;animation:spin 0.7s linear infinite;vertical-align:middle;margin-right:6px;}
@keyframes spin{to{transform:rotate(360deg);}}

/* ===== TIMELINE (dark card, matches reference) ===== */
.timeline-card{background:linear-gradient(180deg,#15161b,#0c0d11);border-radius:18px;padding:24px 22px;
  color:#fff;border:1px solid rgba(255,255,255,0.06);}
.timeline-card .tl-title{font-size:0.72rem;text-transform:uppercase;letter-spacing:0.1em;color:rgba(255,255,255,0.45);
  margin-bottom:14px;font-weight:600;}
.tl-item{display:flex;align-items:flex-start;gap:13px;padding:9px 0;position:relative;}
.tl-item:not(:last-child)::before{content:'';position:absolute;left:9px;top:28px;bottom:-4px;width:1.5px;background:rgba(255,255,255,0.12);}
.tl-item.done:not(:last-child)::before{background:var(--sky-accent);}
.tl-check{width:19px;height:19px;border-radius:50%;flex-shrink:0;display:flex;align-items:center;justify-content:center;
  font-size:0.66rem;border:1.5px solid rgba(255,255,255,0.25);color:rgba(255,255,255,0.3);margin-top:1px;background:#0c0d11;z-index:1;}
.tl-item.done .tl-check{background:var(--sky-accent);border-color:var(--sky-accent);color:#fff;}
.tl-item.current .tl-check{background:#fff;border-color:#fff;color:var(--ink-dark);box-shadow:0 0 0 4px rgba(14,165,233,0.25);}
.tl-body{flex:1;}
.tl-label{font-family:'JetBrains Mono',monospace;font-size:0.86rem;color:rgba(255,255,255,0.4);font-weight:500;}
.tl-item.done .tl-label{color:#fff;}
.tl-item.current .tl-label{color:var(--sky-accent);font-weight:600;}
.tl-time{font-size:0.72rem;color:rgba(255,255,255,0.35);margin-top:2px;font-family:'JetBrains Mono',monospace;}
.tl-note{font-size:0.76rem;color:rgba(255,255,255,0.5);margin-top:3px;}

/* ===== CUSTOMERS ===== */
.cust-list{display:flex;flex-direction:column;gap:10px;}
.cust-card{border:1px solid var(--border);border-radius:10px;padding:16px 18px;display:flex;justify-content:space-between;
  align-items:center;gap:14px;flex-wrap:wrap;cursor:pointer;transition:border-color .15s,background .15s;}
.cust-card:hover{border-color:var(--sky-accent);background:var(--sky-light);}
.cust-card .avatar{width:42px;height:42px;border-radius:50%;background:var(--ink);color:#fff;display:flex;
  align-items:center;justify-content:center;font-weight:700;font-family:'Space Grotesk',sans-serif;flex-shrink:0;}
.cust-info{flex:1;min-width:160px;}
.cust-info .name{font-weight:600;color:var(--ink-dark);font-size:0.95rem;}
.cust-info .meta{font-size:0.8rem;color:var(--slate);margin-top:2px;}
.cust-stats{display:flex;gap:18px;font-size:0.82rem;color:var(--slate);}
.cust-stats b{color:var(--ink-dark);font-family:'Space Grotesk',sans-serif;font-size:1rem;display:block;}

/* ===== REPORTS ===== */
.bar-row{display:flex;align-items:center;gap:12px;padding:9px 0;}
.bar-row .lbl{width:160px;flex-shrink:0;font-size:0.85rem;color:var(--ink-dark);font-weight:500;}
.bar-track{flex:1;height:10px;border-radius:99px;background:var(--sky-light);overflow:hidden;}
.bar-fill{height:100%;border-radius:99px;background:linear-gradient(90deg,var(--ink),var(--sky-accent));}
.bar-row .num{width:34px;text-align:right;font-weight:700;font-size:0.85rem;color:var(--ink-dark);flex-shrink:0;}
.report-grid{display:grid;grid-template-columns:1fr 1fr;gap:24px;}
@media(max-width:900px){.report-grid{grid-template-columns:1fr;}}

@media(max-width:880px){
  .sidebar{transform:translateX(-100%);transition:transform .25s ease;box-shadow:0 0 40px rgba(0,0,0,0.3);}
  .sidebar.open{transform:translateX(0);}
  .content-wrap{margin-left:0;}
  .menu-btn{display:inline-flex;align-items:center;justify-content:center;}
}
</style>
</head>
<body>

<!-- LOGIN -->
<div class="login-wrap" id="loginScreen">
  <div class="login-card">
    <div class="mark">AE</div>
    <h1>ATLAS EXPRESS SERVICE</h1>
    <div class="sub">Admin Portal — Secure Access</div>
    <div class="field">
      <label for="username">Username</label>
      <input type="text" id="username" autocomplete="username" placeholder="admin">
    </div>
    <div class="field">
      <label for="password">Password</label>
      <input type="password" id="password" autocomplete="current-password">
    </div>
    <button class="btn btn-primary" style="width:100%;" onclick="login()">Log in</button>
    <div class="login-error" id="loginError">Incorrect username or password.</div>
  </div>
</div>

<!-- ADMIN SHELL -->
<div class="shell" id="adminShell">

  <aside class="sidebar" id="sidebar">
    <div class="sidebar-brand">
      <span class="mark">AE</span>
      <div>
        <div class="name">ATLAS EXPRESS SERVICE</div>
        <div class="tag">Admin Dashboard</div>
      </div>
    </div>

    <div class="nav-section-label">Main</div>
    <div class="nav-item active" data-tab="dashboard" onclick="switchTab('dashboard',this)"><span class="ic">📦</span> Dashboard</div>
    <div class="nav-item" data-tab="shipments" onclick="switchTab('shipments',this)"><span class="ic">🚚</span> Shipments</div>
    <div class="nav-item" data-tab="create" onclick="switchTab('create',this)"><span class="ic">➕</span> New Shipment</div>
    <div class="nav-item" data-tab="messages" onclick="switchTab('messages',this)"><span class="ic">💬</span> Message AI</div>
    <div class="nav-item" data-tab="customers" onclick="switchTab('customers',this)"><span class="ic">👥</span> Customers</div>
    <div class="nav-item" data-tab="reports" onclick="switchTab('reports',this)"><span class="ic">📈</span> Reports</div>

    <div class="nav-section-label">System</div>
    <div class="nav-item" data-tab="settings" onclick="switchTab('settings',this)"><span class="ic">⚙️</span> Settings</div>

    <div class="sidebar-footer">
      <div class="who">Signed in as<strong id="currentUser">admin</strong></div>
      <button class="btn btn-secondary" style="width:100%;padding:9px 16px;font-size:0.85rem;" onclick="logout()">Log out</button>
    </div>
  </aside>

  <div class="content-wrap">
    <div class="topbar">
      <div style="display:flex;align-items:center;gap:12px;">
        <button class="menu-btn" onclick="document.getElementById('sidebar').classList.toggle('open')">☰</button>
        <div>
          <h1 id="topbarTitle">Dashboard</h1>
          <div class="sub" id="topbarSub">Overview of all shipping activity</div>
        </div>
      </div>
      <div class="topbar-actions">
        <button class="btn btn-outline" style="padding:9px 16px;font-size:0.85rem;" onclick="loadShipments()">↻ Refresh</button>
        <button class="btn btn-primary" style="padding:9px 16px;font-size:0.85rem;" onclick="switchTabByName('create')">➕ New Shipment</button>
      </div>
    </div>

    <main>
      <div class="stats-row">
        <div class="stat-card"><div class="val" id="statTotal">0</div><div class="lbl">Total Shipments</div></div>
        <div class="stat-card"><div class="val" id="statTransit">0</div><div class="lbl">In Transit</div></div>
        <div class="stat-card green"><div class="val" id="statDelivered">0</div><div class="lbl">Delivered</div></div>
        <div class="stat-card slate"><div class="val" id="statPending">0</div><div class="lbl">Pending Pickup</div></div>
      </div>

      <!-- TAB: DASHBOARD -->
      <div class="tab-content active" id="tab-dashboard">
        <div class="grid">
          <div class="panel">
            <h2>Recent Shipments</h2>
            <div class="sub">Latest activity across your network.</div>
            <div class="ship-list" id="dashRecentList"></div>
            <div class="empty-state" id="dashEmpty" style="display:none;">
              <div class="icon">📦</div>
              <div>No shipments yet. Create your first shipment to get started.</div>
            </div>
          </div>
          <div class="panel">
            <h2>Shipments by Stage</h2>
            <div class="sub">Where shipments currently sit in the journey.</div>
            <div id="dashStageBars"></div>
          </div>
        </div>
      </div>

      <!-- TAB: SHIPMENTS -->
      <div class="tab-content" id="tab-shipments">
        <div class="grid">
          <div class="panel" style="margin-bottom:0;">
            <h2>All Shipments</h2>
            <div class="sub">Select a shipment to view details or update tracking.</div>
            <input class="search-bar" type="text" placeholder="🔍 Search by ID, name, or destination…" oninput="filterShipments(this.value)">
            <div class="ship-list" id="shipList"></div>
            <div class="empty-state" id="shipEmpty" style="display:none;">
              <div class="icon">📦</div>
              <div>No shipments yet. Create one in the New Shipment tab.</div>
            </div>
          </div>

          <div class="panel" style="margin-bottom:0;">
            <div id="detailEmpty" class="placeholder-note">Select a shipment from the list to view details and update tracking.</div>
            <div id="detailContent" style="display:none;">
              <div style="display:flex;justify-content:space-between;align-items:flex-start;flex-wrap:wrap;gap:10px;margin-bottom:8px;">
                <div>
                  <h2 id="d_id" style="font-family:'JetBrains Mono',monospace;color:var(--ink-dark);"></h2>
                  <div class="sub" id="d_beneficiary" style="margin-bottom:0;"></div>
                </div>
                <span class="status-badge" id="d_statusBadge"></span>
              </div>

              <!-- Tracking link panel (also shown in detail view) -->
              <div class="track-panel show" style="margin-top:14px;margin-bottom:6px;">
                <div class="track-panel-body" style="padding:16px 18px;gap:14px;">
                  <div class="track-link-box">
                    <div class="lbl">Customer Tracking Link</div>
                    <div class="track-link-row">
                      <input type="text" id="d_trackLink" readonly>
                      <button class="copy-btn" onclick="copyTrackLink('d_trackLink')">Copy</button>
                    </div>
                  </div>
                </div>
              </div>

              <div class="section-title">📋 Tracking Timeline</div>
              <div class="timeline-card" style="margin-bottom:20px;">
                <div class="tl-title">Customer Sees</div>
                <div id="d_timeline"></div>
              </div>

              <div class="section-title">📤 Sender Information</div>
              <div class="detail-row"><span class="k">Name</span><span class="v" id="d_senderName">—</span></div>
              <div class="detail-row"><span class="k">Phone</span><span class="v" id="d_senderPhone">—</span></div>
              <div class="detail-row"><span class="k">Email</span><span class="v" id="d_senderEmail">—</span></div>
              <div class="detail-row"><span class="k">Location</span><span class="v" id="d_origin">—</span></div>

              <div class="section-title">📥 Receiver Information</div>
              <div class="detail-row"><span class="k">Name</span><span class="v" id="d_receiverName">—</span></div>
              <div class="detail-row"><span class="k">Phone</span><span class="v" id="d_receiverPhone">—</span></div>
              <div class="detail-row"><span class="k">Email</span><span class="v" id="d_receiverEmail">—</span></div>
              <div class="detail-row"><span class="k">Location</span><span class="v" id="d_destination">—</span></div>

              <div class="section-title">📦 Shipment Details</div>
              <div class="detail-row"><span class="k">Goods</span><span class="v" id="d_goods">—</span></div>
              <div class="detail-row"><span class="k">Weight</span><span class="v" id="d_weight">—</span></div>
              <div class="detail-row"><span class="k">Item Type</span><span class="v" id="d_itemType">—</span></div>
              <div class="detail-row"><span class="k">Quantity</span><span class="v" id="d_quantity">—</span></div>
              <div class="detail-row"><span class="k">Service</span><span class="v" id="d_service">—</span></div>
              <div class="detail-row"><span class="k">Date Shipped</span><span class="v" id="d_dateShipped">—</span></div>
              <div class="detail-row"><span class="k">Expected Delivery</span><span class="v" id="d_eta">—</span></div>
              <div class="detail-row"><span class="k">Special Notes</span><span class="v" id="d_notes">—</span></div>
              <div class="detail-row"><span class="k">Ready for Pickup</span><span class="v" id="d_pickup">—</span></div>

              <div class="section-title">💰 Payment Management</div>
              <div class="detail-row"><span class="k">Amount To Be Paid</span><span class="v" id="d_amountToPay">—</span></div>
              <div class="detail-row"><span class="k">Amount Paid</span><span class="v" id="d_amountPaid">—</span></div>
              <div class="detail-row"><span class="k">Balance Due</span><span class="v" id="d_balanceDue">—</span></div>
              <div class="detail-row"><span class="k">Payment Status</span><span class="v"><span class="status-badge" id="d_paymentBadge">—</span></span></div>
              <div class="detail-row"><span class="k">Payment Note</span><span class="v" id="d_paymentNote">—</span></div>

              <div class="form-row" style="margin-top:12px;">
                <div class="field-inline"><label>Amount To Be Paid (GBP)</label><input type="text" id="u_amountToPay" placeholder="£150"></div>
                <div class="field-inline"><label>Amount Paid So Far (GBP)</label><input type="text" id="u_amountPaid" placeholder="£0"></div>
                <div class="field-inline"><label>Payment Settlement Status</label><select id="u_paymentStatus"><option>Unpaid</option><option>Partial</option><option>Paid</option><option>Pending Verification</option><option>Refunded</option></select></div>
              </div>
              <div class="field-inline" style="margin-bottom:6px;">
                <label>Payment Note (what this payment is for)</label>
                <textarea id="u_paymentNote" placeholder="e.g. Balance covers customs clearance fee and final-mile delivery charge."></textarea>
              </div>
              <div class="actions-row">
                <button class="btn btn-primary" onclick="updatePayment()">💾 Save Payment Info</button>
              </div>

              <div class="section-title">🔄 Update Tracking</div>
              <div class="form-row">
                <div class="field-inline">
                  <label>Current Stage</label>
                  <select id="u_status"></select>
                </div>
                <div class="field-inline">
                  <label>Location</label>
                  <input type="text" id="u_location" placeholder="e.g. Cairo Sorting Hub, Egypt">
                </div>
              </div>
              <div class="field-inline" style="margin-bottom:6px;">
                <label>Note (optional)</label>
                <input type="text" id="u_note" placeholder="e.g. Cleared customs, awaiting local courier handoff">
              </div>
              <div style="display:flex;align-items:center;gap:10px;padding:10px 0 14px;">
                <input type="checkbox" id="u_pickup" style="width:18px;height:18px;accent-color:var(--sky-accent);cursor:pointer;">
                <label for="u_pickup" style="font-size:0.88rem;font-weight:600;color:var(--ink-dark);cursor:pointer;margin:0;">Mark as Ready for Pickup</label>
              </div>
              <div class="actions-row">
                <button class="btn btn-primary" onclick="addLocationUpdate()">Update Stage</button>
                <button class="btn btn-green" onclick="downloadInvoice()">⬇ Download Invoice</button>
                <button class="btn btn-danger" onclick="deleteShipment()">🗑 Delete</button>
              </div>

              <div style="margin-top:24px;">
                <div class="section-title">🕓 Update History (Internal Log)</div>
                <div id="d_history"></div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- TAB: CREATE -->
      <div class="tab-content" id="tab-create">
        <div class="panel">
          <h2>Create New Shipment</h2>
          <div class="sub">Fill in all details. A tracking ID and customer tracking link will be auto-generated.</div>

          <div class="section-title">📤 Sender Information</div>
          <div class="form-row">
            <div class="field-inline"><label>Sender Full Name *</label><input type="text" id="f_senderName" placeholder="John Adeyemi"></div>
            <div class="field-inline"><label>Sender Phone *</label><input type="tel" id="f_senderPhone" placeholder="+234 800 000 0000"></div>
          </div>
          <div class="form-row">
            <div class="field-inline"><label>Sender Email *</label><input type="email" id="f_senderEmail" placeholder="sender@email.com"></div>
            <div class="field-inline"><label>Sender Location / Address *</label><input type="text" id="f_origin" placeholder="Lagos, Nigeria"></div>
          </div>

          <div class="section-title">📥 Receiver Information</div>
          <div class="form-row">
            <div class="field-inline"><label>Receiver Full Name *</label><input type="text" id="f_receiverName" placeholder="James Smith"></div>
            <div class="field-inline"><label>Receiver Phone *</label><input type="tel" id="f_receiverPhone" placeholder="+44 7700 000000"></div>
          </div>
          <div class="form-row">
            <div class="field-inline"><label>Receiver Email *</label><input type="email" id="f_receiverEmail" placeholder="receiver@email.com"></div>
            <div class="field-inline"><label>Receiver Location / Address *</label><input type="text" id="f_destination" placeholder="London, UK"></div>
          </div>

          <div class="section-title">📦 Goods & Shipment Information</div>
          <div class="form-row">
            <div class="field-inline"><label>Goods Description *</label><input type="text" id="f_goods" placeholder="Electronic devices, clothing…"></div>
            <div class="field-inline"><label>Item Type *</label>
              <select id="f_itemType">
                <option value="">— Select type —</option>
                <option>Documents</option>
                <option>Electronics</option>
                <option>Clothing & Apparel</option>
                <option>Pharmaceuticals</option>
                <option>Perishables / Food</option>
                <option>Furniture</option>
                <option>Automotive Parts</option>
                <option>Jewelry & High-Value</option>
                <option>Industrial Equipment</option>
                <option>Art & Antiques</option>
                <option>General Cargo</option>
                <option>Other</option>
              </select>
            </div>
          </div>
          <div class="form-row-3">
            <div class="field-inline"><label>Weight (kg) *</label><input type="text" id="f_weight" placeholder="3.2 kg"></div>
            <div class="field-inline"><label>Quantity</label><input type="text" id="f_quantity" placeholder="2 boxes"></div>
            <div class="field-inline"><label>Declared Value (GBP)</label><input type="text" id="f_value" placeholder="£500"></div>
            <div class="field-inline"><label>Amount to be Paid (GBP) *</label><input type="text" id="f_amount" placeholder="£150"></div>
          </div>
          <div class="form-row">
            <div class="field-inline"><label>Date Shipped *</label><input type="date" id="f_dateShipped"></div>
            <div class="field-inline"><label>Expected Delivery Date *</label><input type="date" id="f_eta"></div>
          </div>
          <div class="form-row">
            <div class="field-inline"><label>Initial Stage</label>
              <select id="f_status"></select>
            </div>
            <div class="field-inline"><label>Service Type</label>
              <select id="f_service">
                <option>International Air Express</option>
                <option>Ocean Freight</option>
                <option>Rail Freight</option>
                <option>Road Freight</option>
                <option>Same-Day Express</option>
                <option>White Glove Service</option>
              </select>
            </div>
          </div>
          <div class="field-inline" style="margin-bottom:20px;">
            <label>Special Notes / Instructions</label>
            <textarea id="f_notes" placeholder="Fragile — handle with care. Keep refrigerated."></textarea>
          </div>

          <button class="btn btn-primary" style="width:auto;padding:14px 28px;" id="createBtn" onclick="createShipment()">
            Generate Tracking ID & Create Shipment
          </button>

          <!-- Post-creation tracking panel -->
          <div class="track-panel" id="newTrackPanel">
            <div class="track-panel-head">
              <div>
                <div class="label">✅ Shipment Created — Tracking ID</div>
                <div class="id" id="newIdValue">—</div>
              </div>
              <span class="status-badge delivered" style="background:rgba(255,255,255,0.15);color:#fff;">Ready</span>
            </div>
            <div class="track-panel-body">
              <div class="track-link-box">
                <div class="lbl">Share this link with the customer</div>
                <div class="track-link-row">
                  <input type="text" id="newTrackLink" readonly>
                  <button class="copy-btn" onclick="copyTrackLink('newTrackLink')">Copy</button>
                </div>
                <div class="track-link-actions">
                  <button class="copy-btn alt" onclick="openTrackLink()">↗ Open Tracking Page</button>
                  <button class="copy-btn" onclick="downloadLastInvoice()">⬇ Download Invoice</button>
                  <button class="copy-btn ghost" onclick="copyNewId()">Copy ID Only</button>
                </div>
              </div>
              <div class="qr-box">
                <img id="newQr" src="" alt="QR code">
                <div class="cap">Scan to track</div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- TAB: MESSAGES -->
      <div class="tab-content" id="tab-messages">
        <div class="panel">
          <h2>Message AI</h2>
          <div class="sub">Customer messages sent from standalone tracking pages appear here. Select a shipment conversation and reply.</div>
          <div class="grid" style="grid-template-columns:1fr 1.4fr;">
            <div>
              <input class="search-bar" id="messageSearch" placeholder="🔍 Search tracking ID, sender or receiver…" oninput="renderMessageThreads(this.value)">
              <div id="messageThreads" class="ship-list"></div>
            </div>
            <div>
              <div id="messageEmpty" class="placeholder-note">Select a conversation to view messages.</div>
              <div id="messagePanel" style="display:none;">
                <div class="section-title">💬 Conversation</div>
                <div id="messageList" style="max-height:430px;overflow:auto;padding:8px;"></div>
                <div class="field-inline" style="margin-top:12px;">
                  <label>Admin Reply</label>
                  <textarea id="adminReply" placeholder="Write a reply to the customer…"></textarea>
                </div>
                <div class="actions-row"><button class="btn btn-primary" onclick="sendAdminReply()">Send Reply</button></div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- TAB: CUSTOMERS -->
      <div class="tab-content" id="tab-customers">
        <div class="panel">
          <h2>Customers</h2>
          <div class="sub">Everyone who has sent or received a shipment with ATLAS EXPRESS SERVICE, derived automatically from your shipment records.</div>
          <input class="search-bar" type="text" placeholder="🔍 Search customers by name or email…" oninput="filterCustomers(this.value)">
          <div class="cust-list" id="custList"></div>
          <div class="empty-state" id="custEmpty" style="display:none;">
            <div class="icon">👥</div>
            <div>No customers yet — they'll appear here once you create shipments.</div>
          </div>
        </div>
      </div>

      <!-- TAB: REPORTS -->
      <div class="tab-content" id="tab-reports">
        <div class="report-grid">
          <div class="panel">
            <h2>Shipments by Stage</h2>
            <div class="sub">Distribution of all shipments across the tracking journey.</div>
            <div id="repStageBars"></div>
          </div>
          <div class="panel">
            <h2>Shipments by Service Type</h2>
            <div class="sub">Which delivery service is used most.</div>
            <div id="repServiceBars"></div>
          </div>
          <div class="panel">
            <h2>Monthly Shipment Volume</h2>
            <div class="sub">Shipments created per month (last 6 months).</div>
            <div id="repMonthBars"></div>
          </div>
          <div class="panel">
            <h2>Top Destinations</h2>
            <div class="sub">Most common receiver locations.</div>
            <div id="repDestBars"></div>
          </div>
        </div>
      </div>

      <!-- TAB: SETTINGS -->
      <div class="tab-content" id="tab-settings">
        <div class="panel pwd-section" style="max-width:500px;">
          <h2>Change Password</h2>
          <div class="sub">Update your admin login password.</div>
          <div class="field-inline" style="margin-bottom:14px;">
            <label>Current Password</label>
            <input type="password" id="pwd_current" placeholder="Enter current password">
          </div>
          <div class="field-inline" style="margin-bottom:14px;">
            <label>New Password</label>
            <input type="password" id="pwd_new" placeholder="At least 6 characters">
          </div>
          <div class="field-inline" style="margin-bottom:18px;">
            <label>Confirm New Password</label>
            <input type="password" id="pwd_confirm" placeholder="Repeat new password">
          </div>
          <button class="btn btn-primary" onclick="changePassword()">Update Password</button>
          <div class="pwd-status ok" id="pwdOk">✅ Password updated successfully.</div>
          <div class="pwd-status err" id="pwdErr"></div>
        </div>

        <div class="panel" style="max-width:500px;">
          <h2>Admin Credentials</h2>
          <div class="sub">Current login information (read-only display).</div>
          <div class="detail-row"><span class="k">Username</span><span class="v" id="settingsUser">admin</span></div>
          <div class="detail-row"><span class="k">Password</span><span class="v">••••••••</span></div>
          <div class="detail-row"><span class="k">Session</span><span class="v" style="color:var(--sky-accent);">Active ✓</span></div>
        </div>

        <div class="panel" style="max-width:500px;">
          <h2>Customer Tracking Page</h2>
          <div class="sub">Set the public URL where customers can track shipments. Used to build tracking links.</div>
          <div class="field-inline" style="margin-bottom:14px;">
            <label>Tracking Page URL</label>
            <input type="text" id="trackBaseUrl" placeholder="https://yourdomain.com/atlas-express-tracking.html">
          </div>
          <button class="btn btn-primary" onclick="saveTrackBaseUrl()">Save URL</button>
          <div class="pwd-status ok" id="trackUrlOk" style="color:var(--green);">✅ Saved.</div>
        </div>

        <div class="panel" style="max-width:620px;">
          <h2>Shipment Retention</h2>
          <div class="sub">Created shipments remain stored until an administrator intentionally deletes an individual shipment. There is no automatic cleanup or first-login reset.</div>
          <div style="padding:14px 16px;border-radius:10px;background:#E7F8F1;color:#176B50;font-weight:600;">✓ Automatic shipment deletion is disabled.</div>
        </div>
      </div>
    </main>
  </div>
</div>

<div class="toast" id="toast"></div>

<script>
// ═══════════════════════════════════════════════════
// CONFIG — CREDENTIALS
// ═══════════════════════════════════════════════════
const JSONBIN_BIN_ID    = '6a504daaf5f4af5e297a64c8';
const JSONBIN_MASTER_KEY= '$2a$10$HAkKYldNteLU88hcedQVK.GlW8/VQnC1HntBdSHSDyK8ziHgidq4y';
const JSONBIN_BASE       = 'https://api.jsonbin.io/v3/b/' + JSONBIN_BIN_ID;

const SESSION_KEY = 'gd_admin_session';
const CREDS_KEY   = 'gd_admin_creds';
const TRACK_URL_KEY = 'gd_track_base_url';

// 9-stage customer-facing tracking journey
const STAGES = ['Shipment Created','Picked Up','Warehouse','Airport','Customs',
  'Destination Country','Local Distribution','Out for Delivery','Delivered'];

function getTrackBaseUrl() {
  return localStorage.getItem(TRACK_URL_KEY) ||
    (window.location.href.replace(/[^\/]*$/, '') + 'atlas-express-tracking.html');
}
function saveTrackBaseUrl() {
  const v = document.getElementById('trackBaseUrl').value.trim();
  if (v) localStorage.setItem(TRACK_URL_KEY, v);
  const ok = document.getElementById('trackUrlOk');
  ok.style.display = 'block';
  setTimeout(() => ok.style.display = 'none', 2500);
  showToast('Tracking page URL saved', 'success');
}
function buildTrackLink(id) {
  return getTrackBaseUrl() + '?id=' + encodeURIComponent(id);
}

function populateStageSelects() {
  ['u_status','f_status'].forEach(selId => {
    const sel = document.getElementById(selId);
    sel.innerHTML = STAGES.map(s => `<option value="${s}">${s}</option>`).join('');
  });
  document.getElementById('f_status').value = 'Shipment Created';
}
populateStageSelects();

function getCreds() {
  try { return JSON.parse(localStorage.getItem(CREDS_KEY)) || { user:'admin', pass:'admin3030' }; }
  catch(e) { return { user:'admin', pass:'admin3030' }; }
}
function saveCreds(c) { localStorage.setItem(CREDS_KEY, JSON.stringify(c)); }

function login() {
  const u = document.getElementById('username').value.trim();
  const p = document.getElementById('password').value;
  const creds = getCreds();
  if (u === creds.user && p === creds.pass) {
    sessionStorage.setItem(SESSION_KEY, '1');
    document.getElementById('loginError').style.display = 'none';
    showAdmin();
  } else {
    document.getElementById('loginError').style.display = 'block';
  }
}
function logout() {
  sessionStorage.removeItem(SESSION_KEY);
  document.getElementById('loginScreen').style.display = 'flex';
  document.getElementById('adminShell').style.display = 'none';
  document.getElementById('password').value = '';
}
async function showAdmin() {
  document.getElementById('loginScreen').style.display = 'none';
  document.getElementById('adminShell').style.display = 'block';
  document.getElementById('currentUser').textContent = getCreds().user;
  document.getElementById('settingsUser').textContent = getCreds().user;
  document.getElementById('trackBaseUrl').value = getTrackBaseUrl();
  loadShipments();
}
document.getElementById('password').addEventListener('keydown', e => { if(e.key==='Enter') login(); });
if (sessionStorage.getItem(SESSION_KEY) === '1') showAdmin();

function changePassword() {
  const cur  = document.getElementById('pwd_current').value;
  const nw   = document.getElementById('pwd_new').value;
  const conf = document.getElementById('pwd_confirm').value;
  const ok   = document.getElementById('pwdOk');
  const err  = document.getElementById('pwdErr');
  ok.style.display = 'none'; err.style.display = 'none';
  const creds = getCreds();
  if (cur !== creds.pass) { err.textContent = '❌ Current password is incorrect.'; err.style.display = 'block'; return; }
  if (nw.length < 6)      { err.textContent = '❌ New password must be at least 6 characters.'; err.style.display = 'block'; return; }
  if (nw !== conf)         { err.textContent = '❌ Passwords do not match.'; err.style.display = 'block'; return; }
  saveCreds({ user: creds.user, pass: nw });
  document.getElementById('pwd_current').value = '';
  document.getElementById('pwd_new').value = '';
  document.getElementById('pwd_confirm').value = '';
  ok.style.display = 'block';
  showToast('Password updated successfully!', 'success');
}

const TAB_META = {
  dashboard: ['Dashboard','Overview of all shipping activity'],
  shipments: ['Shipments','Manage and update every shipment'],
  create:    ['New Shipment','Register a new shipment and notify the customer'],
  customers: ['Customers','Everyone who has shipped with you'],
  reports:   ['Reports','Performance and volume insights'],
  settings:  ['Settings','Account and tracking page configuration'],
};

function switchTab(name, el) {
  document.querySelectorAll('.nav-item').forEach(b => b.classList.remove('active'));
  document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('active'));
  document.getElementById('tab-' + name).classList.add('active');
  if (el) el.classList.add('active');
  else document.querySelector(`.nav-item[data-tab="${name}"]`).classList.add('active');
  const meta = TAB_META[name];
  if (meta) {
    document.getElementById('topbarTitle').textContent = meta[0];
    document.getElementById('topbarSub').textContent = meta[1];
  }
  document.getElementById('sidebar').classList.remove('open');
  if (name === 'dashboard') renderDashboard();
  if (name === 'customers') renderCustomers();
  if (name === 'messages') renderMessageThreads();
  if (name === 'reports') renderReports();
}
function switchTabByName(name) { switchTab(name, document.querySelector(`.nav-item[data-tab="${name}"]`)); }

function showToast(msg, type='info') {
  const t = document.getElementById('toast');
  t.textContent = msg;
  t.className = 'toast ' + type + ' show';
  setTimeout(() => t.classList.remove('show'), 3500);
}


let selectedMessageId = null;
function getMessagesFor(s){ return Array.isArray(s.messages) ? s.messages : []; }
function renderMessageThreads(filter=''){
  const box=document.getElementById('messageThreads'); if(!box) return;
  const f=(filter||'').toLowerCase();
  const ships=Object.values(allShipments).filter(s=>getMessagesFor(s).length && JSON.stringify(s).toLowerCase().includes(f));
  box.innerHTML=ships.length?ships.map(s=>{
    const msgs=getMessagesFor(s), last=msgs[msgs.length-1];
    return `<div class="ship-card ${selectedMessageId===s.id?'active':''}" onclick="selectMessageThread('${esc(s.id)}')">
      <div class="top"><span class="id">${esc(s.id)}</span><span class="status-badge transit">${msgs.length} message${msgs.length===1?'':'s'}</span></div>
      <div class="route">${esc(s.senderName)} → ${esc(s.receiverName)}</div>
      <div class="route" style="margin-top:4px;">${esc(last?.text||'')}</div>
    </div>`;
  }).join(''):'<div class="empty-state">No customer messages yet.</div>';
}
function selectMessageThread(id){
  selectedMessageId=id;
  document.getElementById('messageEmpty').style.display='none';
  document.getElementById('messagePanel').style.display='block';
  renderMessageThreads(document.getElementById('messageSearch')?.value||'');
  const s=allShipments[id], msgs=getMessagesFor(s);
  document.getElementById('messageList').innerHTML=msgs.map(x=>`<div style="margin:8px 0;padding:10px 12px;border-radius:10px;background:${x.from==='admin'?'#E0F2FE':'#F0F7FF'};border-left:3px solid ${x.from==='admin'?'#0EA5E9':'#1A3F72'};"><b>${x.from==='admin'?'Admin':'Customer'}</b><div>${esc(x.text)}</div><small>${fmtDate(x.time)}</small></div>`).join('');
  document.getElementById('messageList').scrollTop=999999;
}
async function sendAdminReply(){
  if(!selectedMessageId) return;
  const text=document.getElementById('adminReply').value.trim(); if(!text) return;
  const s=allShipments[selectedMessageId]; s.messages=getMessagesFor(s);
  s.messages.push({from:'admin',text,time:new Date().toISOString()});
  await saveBin({shipments:allShipments});
  document.getElementById('adminReply').value='';
  selectMessageThread(selectedMessageId);
  showToast('Reply sent to tracking page ✓','success');
}
async function fetchBin() {
  const r = await fetch(JSONBIN_BASE + '/latest', {
    headers: { 'X-Master-Key': JSONBIN_MASTER_KEY }
  });
  const d = await r.json();
  return d.record || { shipments: {} };
}
async function saveBin(data) {
  await fetch(JSONBIN_BASE, {
    method: 'PUT',
    headers: { 'Content-Type':'application/json', 'X-Master-Key': JSONBIN_MASTER_KEY },
    body: JSON.stringify(data)
  });
}

let allShipments = {};
let selectedId   = null;
let lastCreated  = null;

async function loadShipments() {
  showToast('Loading shipments…', 'info');
  try {
    const data = await fetchBin();
    allShipments = data.shipments || {};
    renderList();
    updateStats();
    renderDashboard();
    if (document.getElementById('tab-customers').classList.contains('active')) renderCustomers();
    if (document.getElementById('tab-reports').classList.contains('active')) renderReports();
    if (selectedId) renderDetail();
    showToast('Shipments loaded ✓', 'success');
  } catch(e) {
    showToast('Failed to load shipments. Check connection.', 'error');
  }
}

function generateTrackingId() {
  const year = new Date().getFullYear();
  let id = `ATEX-${year}-${Math.floor(10000 + Math.random() * 90000)}`;
  while (allShipments[id]) {
    id = `ATEX-${year}-${Math.floor(10000 + Math.random() * 90000)}`;
  }
  return id;
}

async function createShipment() {
  const get = id => document.getElementById(id).value.trim();
  const required = [
    ['f_senderName','Sender Name'],['f_senderPhone','Sender Phone'],
    ['f_senderEmail','Sender Email'],['f_origin','Sender Location'],
    ['f_receiverName','Receiver Name'],['f_receiverPhone','Receiver Phone'],
    ['f_receiverEmail','Receiver Email'],['f_destination','Receiver Location'],
    ['f_goods','Goods Description'],['f_weight','Weight'],
    ['f_dateShipped','Date Shipped'],['f_eta','Expected Delivery Date'],
    ['f_amount','Amount to be Paid'],
  ];
  for (const [id, label] of required) {
    if (!get(id)) { showToast(`Please fill in: ${label}`, 'error'); return; }
  }
  const btn = document.getElementById('createBtn');
  btn.innerHTML = '<span class="spinner"></span> Creating…';
  btn.disabled = true;

  const id = generateTrackingId();
  const nowIso = new Date().toISOString();
  const initialStage = document.getElementById('f_status').value || 'Shipment Created';
  const shipment = {
    id,
    senderName   : get('f_senderName'),
    senderPhone  : get('f_senderPhone'),
    senderEmail  : get('f_senderEmail'),
    origin       : get('f_origin'),
    receiverName : get('f_receiverName'),
    receiverPhone: get('f_receiverPhone'),
    receiverEmail: get('f_receiverEmail'),
    destination  : get('f_destination'),
    goods        : get('f_goods'),
    itemType     : get('f_itemType'),
    weight       : get('f_weight'),
    quantity     : get('f_quantity') || '1',
    declaredValue: get('f_value') || 'N/A',
    amountToPay  : get('f_amount') || 'N/A',
    amountPaid   : '£0',
    paymentStatus: 'Unpaid',
    paymentNote  : '',
    dateShipped  : get('f_dateShipped'),
    eta          : get('f_eta'),
    status       : initialStage,
    service      : document.getElementById('f_service').value,
    readyForPickup: false,
    notes        : get('f_notes') || 'None',
    created      : nowIso,
    history      : [{ location: get('f_origin'), status: initialStage, time: nowIso, note: 'Shipment registered with ATLAS EXPRESS SERVICE.' }],
    messages    : []
  };

  allShipments[id] = shipment;
  lastCreated = shipment;

  try {
    await saveBin({ shipments: allShipments });
    showTrackPanel(id);
    resetCreateForm();
    renderList();
    updateStats();
    renderDashboard();
    showToast('Shipment created: ' + id, 'success');
  } catch(e) {
    showToast('Error saving shipment. Please try again.', 'error');
  }
  btn.innerHTML = 'Generate Tracking ID & Create Shipment';
  btn.disabled = false;
}

function showTrackPanel(id) {
  const link = buildTrackLink(id);
  document.getElementById('newIdValue').textContent = id;
  document.getElementById('newTrackLink').value = link;
  document.getElementById('newQr').src = 'https://api.qrserver.com/v1/create-qr-code/?size=140x140&margin=4&data=' + encodeURIComponent(link);
  document.getElementById('newTrackPanel').classList.add('show');
}

function resetCreateForm() {
  ['f_senderName','f_senderPhone','f_senderEmail','f_origin',
   'f_receiverName','f_receiverPhone','f_receiverEmail','f_destination',
   'f_goods','f_weight','f_quantity','f_value','f_amount','f_dateShipped','f_eta','f_notes']
    .forEach(id => document.getElementById(id).value = '');
  document.getElementById('f_itemType').value = '';
  document.getElementById('f_status').value = 'Shipment Created';
  document.getElementById('f_service').value = 'International Air Express';
}

function copyNewId() {
  navigator.clipboard.writeText(document.getElementById('newIdValue').textContent)
    .then(() => showToast('Tracking ID copied!', 'success'));
}
function copyTrackLink(inputId) {
  const el = document.getElementById(inputId);
  navigator.clipboard.writeText(el.value).then(() => showToast('Tracking link copied!', 'success'));
}
function openTrackLink() {
  window.open(document.getElementById('newTrackLink').value, '_blank');
}

function updateStats() {
  const all = Object.values(allShipments);
  document.getElementById('statTotal').textContent     = all.length;
  document.getElementById('statTransit').textContent   = all.filter(s => {
    const i = STAGES.indexOf(s.status);
    return i > 0 && i < STAGES.length - 1;
  }).length;
  document.getElementById('statDelivered').textContent = all.filter(s => s.status === 'Delivered').length;
  document.getElementById('statPending').textContent   = all.filter(s => s.status === 'Shipment Created').length;
}

function renderList(filter='') {
  const list = Object.values(allShipments)
    .filter(s => !filter || JSON.stringify(s).toLowerCase().includes(filter.toLowerCase()))
    .sort((a,b) => new Date(b.created) - new Date(a.created));
  const container = document.getElementById('shipList');
  const empty     = document.getElementById('shipEmpty');
  container.innerHTML = '';
  if (list.length === 0) { empty.style.display = 'block'; return; }
  empty.style.display = 'none';
  list.forEach(s => container.appendChild(buildShipCard(s)));
}

function buildShipCard(s, onClickOverride) {
  const div = document.createElement('div');
  div.className = 'ship-card' + (s.id === selectedId ? ' active' : '');
  div.onclick = onClickOverride || (() => selectShipment(s.id));
  div.innerHTML = `
    <div class="top">
      <span class="id">${esc(s.id)}</span>
      <span class="status-badge ${statusClass(s.status)}">${esc(s.status)}</span>
    </div>
    <div class="route">${esc(s.origin)} → ${esc(s.destination)}</div>
    <div class="route" style="margin-top:3px;">📦 ${esc(s.goods)} · 👤 ${esc(s.receiverName)}</div>
    <div class="ship-card-actions">
      <button type="button" class="mini-btn" onclick="event.stopPropagation(); viewShipmentCard('${esc(s.id)}')">👁 View</button>
      <button type="button" class="mini-btn mini-green" onclick="event.stopPropagation(); downloadInvoiceCard('${esc(s.id)}')">⬇ Invoice</button>
      <button type="button" class="mini-btn mini-danger" onclick="event.stopPropagation(); deleteShipmentCard('${esc(s.id)}')">🗑 Delete</button>
    </div>
  `;
  return div;
}

function viewShipmentCard(id) {
  selectShipment(id);
}

function downloadInvoiceCard(id) {
  const s = allShipments[id];
  if (s) generateInvoice(s);
  else showToast('Shipment not found.', 'error');
}

async function deleteShipmentCard(id) {
  const s = allShipments[id];
  if (!s) return;
  if (!confirm('Delete shipment ' + id + '? This cannot be undone.')) return;

  // Remove locally and refresh the UI immediately so it's visibly gone right away
  delete allShipments[id];
  if (selectedId === id) selectedId = null;
  const searchVal = document.querySelector('#tab-shipments .search-bar') ? document.querySelector('#tab-shipments .search-bar').value : '';
  renderList(searchVal);
  renderDetail();
  updateStats();
  renderDashboard();
  showToast('Shipment deleted.', 'info');

  // Persist the change to the backend
  try {
    await saveBin({ shipments: allShipments });
  } catch (e) {
    showToast('Deleted locally, but failed to sync — check connection.', 'error');
  }
}

async function clearAllShipments() {
  showToast('Automatic/bulk shipment deletion is disabled. Delete shipments individually from their shipment card.', 'info');
}

function filterShipments(q) { renderList(q); }

function selectShipment(id) {
  selectedId = id;
  renderList(document.querySelector('#tab-shipments .search-bar').value);
  renderDetail();
  switchTabByName('shipments');
}

function renderDetail() {
  const s = allShipments[selectedId];
  const empty   = document.getElementById('detailEmpty');
  const content = document.getElementById('detailContent');
  if (!s) { empty.style.display = 'block'; content.style.display = 'none'; selectedId = null; return; }
  empty.style.display = 'none';
  content.style.display = 'block';

  setText('d_id', s.id);
  setText('d_beneficiary', 'For ' + s.receiverName + ' · ' + s.receiverEmail);
  const badge = document.getElementById('d_statusBadge');
  badge.textContent = s.status;
  badge.className = 'status-badge ' + statusClass(s.status);

  document.getElementById('d_trackLink').value = buildTrackLink(s.id);

  document.getElementById('d_timeline').innerHTML = renderTimelineHtml(s);

  setText('d_senderName',   s.senderName);
  setText('d_senderPhone',  s.senderPhone);
  setText('d_senderEmail',  s.senderEmail);
  setText('d_origin',       s.origin);
  setText('d_receiverName', s.receiverName);
  setText('d_receiverPhone',s.receiverPhone);
  setText('d_receiverEmail',s.receiverEmail);
  setText('d_destination',  s.destination);
  setText('d_goods',        s.goods);
  setText('d_weight',       s.weight);
  setText('d_itemType',     s.itemType || '—');
  setText('d_quantity',     s.quantity || '—');
  setText('d_service',      s.service || '—');
  setText('d_dateShipped',  fmtDateOnly(s.dateShipped));
  setText('d_eta',          fmtDateOnly(s.eta));
  setText('d_notes',        s.notes || '—');
  setText('d_pickup',       s.readyForPickup ? '✅ Yes' : 'No');

  const totalAmt = parseMoney(s.amountToPay);
  const paidAmt  = parseMoney(s.amountPaid) || 0;
  const balance  = totalAmt != null ? Math.max(totalAmt - paidAmt, 0) : null;
  const payStatus = (totalAmt != null && totalAmt > 0 && paidAmt >= totalAmt) ? 'Paid'
    : (paidAmt > 0 ? 'Partial' : 'Unpaid');

  setText('d_amountToPay', s.amountToPay || 'N/A');
  setText('d_amountPaid',  fmtMoney(paidAmt));
  setText('d_balanceDue',  balance != null ? fmtMoney(balance) : 'N/A');
  const explicitPaymentStatus = s.paymentStatus || payStatus;
  const payBadge = document.getElementById('d_paymentBadge');
  payBadge.textContent = explicitPaymentStatus;
  payBadge.className = 'status-badge ' + (explicitPaymentStatus === 'Paid' ? 'delivered' : explicitPaymentStatus === 'Partial' ? 'customs' : explicitPaymentStatus === 'Pending Verification' ? 'transit' : 'pending');
  setText('d_paymentNote', s.paymentNote || '—');

  document.getElementById('u_amountToPay').value = (s.amountToPay && s.amountToPay !== 'N/A') ? s.amountToPay : '';
  document.getElementById('u_amountPaid').value  = (s.amountPaid && s.amountPaid !== '£0') ? s.amountPaid : '';
  document.getElementById('u_paymentStatus').value = s.paymentStatus || payStatus;
  document.getElementById('u_paymentNote').value = s.paymentNote || '';

  document.getElementById('u_location').value = '';
  document.getElementById('u_note').value = '';
  document.getElementById('u_status').value   = s.status;
  document.getElementById('u_pickup').checked  = !!s.readyForPickup;

  const histEl = document.getElementById('d_history');
  histEl.innerHTML = '';
  const items = (s.history || []).slice().reverse();
  if (items.length === 0) {
    histEl.innerHTML = `<div class="detail-row"><span class="k">Shipment created</span><span class="v">${fmtDate(s.created)}</span></div>`;
  } else {
    items.forEach(h => {
      const div = document.createElement('div');
      div.className = 'detail-row';
      div.innerHTML = `<span class="k">${esc(h.location)} <span style="color:var(--sky-accent);">[${esc(h.status||'')}]</span>${h.note ? '<br><span style="font-size:0.78rem;color:var(--slate);">'+esc(h.note)+'</span>' : ''}</span><span class="v" style="font-weight:500;">${fmtDate(h.time)}</span>`;
      histEl.appendChild(div);
    });
  }
}

function renderTimelineHtml(s) {
  const curIdx = STAGES.indexOf(s.status);
  const stageTimes = {};
  const stageNotes = {};
  (s.history || []).forEach(h => {
    if (STAGES.includes(h.status)) { stageTimes[h.status] = h.time; stageNotes[h.status] = h.note; }
  });
  if (!stageTimes['Shipment Created']) stageTimes['Shipment Created'] = s.created;

  return STAGES.map((stage, i) => {
    const done = curIdx >= 0 && i <= curIdx;
    const isCurrent = i === curIdx;
    const time = stageTimes[stage];
    const note = stageNotes[stage];
    return `<div class="tl-item ${done ? 'done' : ''} ${isCurrent ? 'current' : ''}">
      <div class="tl-check">${done ? '✔' : ''}</div>
      <div class="tl-body">
        <div class="tl-label">${esc(stage)}</div>
        ${time ? `<div class="tl-time">${fmtDate(time)}</div>` : ''}
        ${note ? `<div class="tl-note">${esc(note)}</div>` : ''}
      </div>
    </div>`;
  }).join('');
}

async function addLocationUpdate() {
  if (!selectedId) return;
  const location = document.getElementById('u_location').value.trim();
  const status   = document.getElementById('u_status').value;
  const note     = document.getElementById('u_note').value.trim();
  const pickup   = document.getElementById('u_pickup').checked;
  const s = allShipments[selectedId];
  if (!s) return;
  s.history = s.history || [];
  s.history.push({ location: location || s.destination || s.origin, status, time: new Date().toISOString(), note });
  s.status = status;
  s.readyForPickup = pickup;
  try {
    await saveBin({ shipments: allShipments });
    renderList(document.querySelector('#tab-shipments .search-bar').value);
    renderDetail();
    updateStats();
    showToast('Tracking updated to "' + status + '" ✓', 'success');
  } catch(e) {
    showToast('Error saving update.', 'error');
  }
}

async function updatePayment() {
  if (!selectedId) return;
  const s = allShipments[selectedId];
  if (!s) return;
  const amountToPay = document.getElementById('u_amountToPay').value.trim();
  const amountPaid  = document.getElementById('u_amountPaid').value.trim();
  const paymentStatus = document.getElementById('u_paymentStatus').value;
  const paymentNote = document.getElementById('u_paymentNote').value.trim();
  s.amountToPay = amountToPay || s.amountToPay || 'N/A';
  s.amountPaid  = amountPaid || '£0';
  s.paymentStatus = paymentStatus || 'Unpaid';
  s.paymentNote = paymentNote;
  try {
    await saveBin({ shipments: allShipments });
    renderDetail();
    renderList(document.querySelector('#tab-shipments .search-bar').value);
    showToast('Payment info updated ✓', 'success');
  } catch(e) {
    showToast('Error saving payment update.', 'error');
  }
}

async function deleteShipment() {
  if (!selectedId) return;
  if (!confirm('Delete shipment ' + selectedId + '? This cannot be undone.')) return;
  const id = selectedId;
  delete allShipments[id];
  selectedId = null;
  renderList();
  renderDetail();
  updateStats();
  renderDashboard();
  showToast('Shipment deleted.', 'info');
  try {
    await saveBin({ shipments: allShipments });
  } catch(e) {
    showToast('Deleted locally, but failed to sync — check connection.', 'error');
  }
}

function downloadLastInvoice() {
  if (lastCreated) generateInvoice(lastCreated);
  else showToast('No shipment to generate invoice for.', 'error');
}
function downloadInvoice() {
  const s = allShipments[selectedId];
  if (s) generateInvoice(s);
}

// ═══════════════════════════════════════════════════
// DASHBOARD / CUSTOMERS / REPORTS
// ═══════════════════════════════════════════════════
function renderDashboard() {
  const all = Object.values(allShipments).sort((a,b) => new Date(b.created) - new Date(a.created));
  const container = document.getElementById('dashRecentList');
  const empty = document.getElementById('dashEmpty');
  container.innerHTML = '';
  if (all.length === 0) { empty.style.display = 'block'; }
  else {
    empty.style.display = 'none';
    all.slice(0, 6).forEach(s => container.appendChild(buildShipCard(s, () => selectShipment(s.id))));
  }
  const counts = STAGES.map(stage => all.filter(s => s.status === stage).length);
  const max = Math.max(1, ...counts);
  document.getElementById('dashStageBars').innerHTML = STAGES.map((stage, i) => `
    <div class="bar-row">
      <div class="lbl">${stage}</div>
      <div class="bar-track"><div class="bar-fill" style="width:${(counts[i]/max*100).toFixed(0)}%;"></div></div>
      <div class="num">${counts[i]}</div>
    </div>`).join('');
}

function renderCustomers(filter='') {
  const map = {};
  Object.values(allShipments).forEach(s => {
    [{ name: s.senderName, email: s.senderEmail, phone: s.senderPhone, role: 'Sender' },
     { name: s.receiverName, email: s.receiverEmail, phone: s.receiverPhone, role: 'Receiver' }]
    .forEach(p => {
      if (!p.email) return;
      const key = p.email.toLowerCase();
      if (!map[key]) map[key] = { name: p.name, email: p.email, phone: p.phone, roles: new Set(), shipments: 0 };
      map[key].roles.add(p.role);
      map[key].shipments++;
    });
  });
  let customers = Object.values(map);
  if (filter) {
    const f = filter.toLowerCase();
    customers = customers.filter(c => c.name.toLowerCase().includes(f) || c.email.toLowerCase().includes(f));
  }
  customers.sort((a,b) => b.shipments - a.shipments);

  const container = document.getElementById('custList');
  const empty = document.getElementById('custEmpty');
  container.innerHTML = '';
  if (customers.length === 0) { empty.style.display = 'block'; return; }
  empty.style.display = 'none';
  customers.forEach(c => {
    const initials = c.name.split(' ').map(w => w[0]).slice(0,2).join('').toUpperCase();
    const div = document.createElement('div');
    div.className = 'cust-card';
    div.onclick = () => {
      switchTabByName('shipments');
      const bar = document.querySelector('#tab-shipments .search-bar');
      bar.value = c.email;
      filterShipments(c.email);
    };
    div.innerHTML = `
      <div class="avatar">${esc(initials || '?')}</div>
      <div class="cust-info">
        <div class="name">${esc(c.name)}</div>
        <div class="meta">${esc(c.email)} · ${esc(c.phone || '—')} · ${[...c.roles].join(' & ')}</div>
      </div>
      <div class="cust-stats"><div><b>${c.shipments}</b>shipment${c.shipments!==1?'s':''}</div></div>
    `;
    container.appendChild(div);
  });
}
function filterCustomers(q) { renderCustomers(q); }

function renderReports() {
  const all = Object.values(allShipments);

  const stageCounts = STAGES.map(stage => all.filter(s => s.status === stage).length);
  const stageMax = Math.max(1, ...stageCounts);
  document.getElementById('repStageBars').innerHTML = STAGES.map((stage, i) => `
    <div class="bar-row"><div class="lbl">${stage}</div>
      <div class="bar-track"><div class="bar-fill" style="width:${(stageCounts[i]/stageMax*100).toFixed(0)}%;"></div></div>
      <div class="num">${stageCounts[i]}</div></div>`).join('') || '<div class="placeholder-note">No data yet.</div>';

  const services = {};
  all.forEach(s => { const k = s.service || 'Unspecified'; services[k] = (services[k]||0)+1; });
  const svcEntries = Object.entries(services).sort((a,b) => b[1]-a[1]);
  const svcMax = Math.max(1, ...svcEntries.map(e => e[1]));
  document.getElementById('repServiceBars').innerHTML = svcEntries.map(([k,v]) => `
    <div class="bar-row"><div class="lbl">${esc(k)}</div>
      <div class="bar-track"><div class="bar-fill" style="width:${(v/svcMax*100).toFixed(0)}%;"></div></div>
      <div class="num">${v}</div></div>`).join('') || '<div class="placeholder-note">No data yet.</div>';

  const months = [];
  const now = new Date();
  for (let i = 5; i >= 0; i--) {
    const d = new Date(now.getFullYear(), now.getMonth() - i, 1);
    months.push({ key: d.getFullYear()+'-'+d.getMonth(), label: d.toLocaleDateString(undefined,{month:'short',year:'2-digit'}), count:0 });
  }
  all.forEach(s => {
    const d = new Date(s.created);
    const key = d.getFullYear()+'-'+d.getMonth();
    const m = months.find(m => m.key === key);
    if (m) m.count++;
  });
  const monthMax = Math.max(1, ...months.map(m => m.count));
  document.getElementById('repMonthBars').innerHTML = months.map(m => `
    <div class="bar-row"><div class="lbl">${m.label}</div>
      <div class="bar-track"><div class="bar-fill" style="width:${(m.count/monthMax*100).toFixed(0)}%;"></div></div>
      <div class="num">${m.count}</div></div>`).join('');

  const dest = {};
  all.forEach(s => { const k = s.destination || 'Unspecified'; dest[k] = (dest[k]||0)+1; });
  const destEntries = Object.entries(dest).sort((a,b) => b[1]-a[1]).slice(0,8);
  const destMax = Math.max(1, ...destEntries.map(e => e[1]));
  document.getElementById('repDestBars').innerHTML = destEntries.map(([k,v]) => `
    <div class="bar-row"><div class="lbl">${esc(k)}</div>
      <div class="bar-track"><div class="bar-fill" style="width:${(v/destMax*100).toFixed(0)}%;"></div></div>
      <div class="num">${v}</div></div>`).join('') || '<div class="placeholder-note">No data yet.</div>';
}

// ═══════════════════════════════════════════════════
// INVOICE JPG (rendered on a canvas, downloaded as .jpg)
// ═══════════════════════════════════════════════════
function rrPath(ctx, x, y, w, h, r) {
  ctx.beginPath();
  ctx.moveTo(x + r, y);
  ctx.arcTo(x + w, y, x + w, y + h, r);
  ctx.arcTo(x + w, y + h, x, y + h, r);
  ctx.arcTo(x, y + h, x, y, r);
  ctx.arcTo(x, y, x + w, y, r);
  ctx.closePath();
}
function fillRR(ctx, x, y, w, h, r, color) {
  if (color) ctx.fillStyle = color;
  rrPath(ctx, x, y, w, h, r);
  ctx.fill();
}
function strokeRR(ctx, x, y, w, h, r, color, lineWidth) {
  ctx.strokeStyle = color;
  ctx.lineWidth = lineWidth;
  rrPath(ctx, x, y, w, h, r);
  ctx.stroke();
}

function drawWrappedText(ctx, text, x, y, maxWidth, lineHeight, maxLines=6){
  text = String(text ?? '').replace(/\s+/g, ' ').trim();
  if (!text) return y;
  const words = text.split(' ');
  const lines = [];
  let line = '';
  for (const word of words) {
    let candidate = line ? line + ' ' + word : word;
    if (ctx.measureText(candidate).width <= maxWidth) {
      line = candidate;
    } else if (line) {
      lines.push(line);
      line = word;
      if (lines.length === maxLines) break;
    } else {
      // Break a single over-wide word by character so even unbroken text stays inside the cell.
      let part = '';
      for (const ch of word) {
        const test = part + ch;
        if (ctx.measureText(test).width > maxWidth && part) {
          lines.push(part);
          part = ch;
          if (lines.length === maxLines) break;
        } else part = test;
      }
      if (lines.length < maxLines) line = part;
      if (lines.length === maxLines) break;
    }
  }
  if (line && lines.length < maxLines) lines.push(line);
  const clipped = lines.length < words.length || (line && lines.length === maxLines && lines.join(' ') !== text);
  if (clipped && lines.length) {
    let last = lines[lines.length - 1];
    while (last && ctx.measureText(last + '…').width > maxWidth) last = last.slice(0, -1);
    lines[lines.length - 1] = last + '…';
  }
  lines.forEach((ln,i) => ctx.fillText(ln, x, y + i * lineHeight));
  return y + lines.length * lineHeight;
}

function drawFittedText(ctx, text, x, y, maxWidth, startPx, minPx=8, fontFamily=FONT, weight='') {
  const value = String(text ?? '');
  let size = startPx;
  while (size > minPx) {
    ctx.font = `${weight ? weight + ' ' : ''}${size}px ${fontFamily}`;
    if (ctx.measureText(value).width <= maxWidth) break;
    size -= 0.35;
  }
  if (ctx.measureText(value).width <= maxWidth) { ctx.fillText(value, x, y); return; }
  let out = value;
  while (out.length && ctx.measureText(out + '…').width > maxWidth) out = out.slice(0, -1);
  ctx.fillText(out + '…', x, y);
}

function drawLabeledCell(ctx, label, value, x, y, width, options={}) {
  const labelW = options.labelW || 21;
  const valueX = x + labelW;
  const valueW = Math.max(10, width - labelW);
  const lineH = options.lineH || 5.8;
  ctx.save();
  ctx.beginPath(); ctx.rect(x, y - 5, width, (options.height || 8) + 6); ctx.clip();
  ctx.textAlign = 'left';
  ctx.fillStyle = 'rgb(40,55,80)';
  ctx.font = `${FS(options.labelSize || 8.1)}px ${FONT}`;
  ctx.fillText(label + ':', px(x), px(y));
  ctx.fillStyle = 'rgb(40,55,80)';
  ctx.font = `${FS(options.valueSize || 8.0)}px ${FONT}`;
  drawWrappedText(ctx, value, px(valueX), px(y), px(valueW), px(lineH), options.maxLines || 1);
  ctx.restore();
}

async function generateInvoice(s) {
  if (!window.jspdf) {
    showToast('PDF engine still loading — please try again in a moment.', 'error');
    return;
  }
  try { await document.fonts.load('700 40px Caveat'); await document.fonts.load('600 24px Caveat'); } catch (e) {}

  let qrImg = null;
  try {
    qrImg = await new Promise((resolve, reject) => {
      const img = new Image();
      img.crossOrigin = 'anonymous';
      img.onload = () => resolve(img);
      img.onerror = reject;
      img.src = 'https://api.qrserver.com/v1/create-qr-code/?size=240x240&margin=4&data=' + encodeURIComponent(buildTrackLink(s.id));
    });
  } catch (e) { qrImg = null; }

  const SCALE = 4;
  const PW = 210, PH = 297, M = 16, CW = PW - M * 2;
  const px = mm => mm * SCALE;
  const FS = pt => pt * 0.3528 * SCALE;
  const FONT = 'Helvetica, Arial, sans-serif';
  const SIGFONT = "'Caveat', cursive";

  // Invoice-safe text helpers. Every value originating from the shipment form is
  // either wrapped inside its allocated box/cell or fitted/ellipsized inside it.
  function clean(v) { return String(v ?? '').replace(/\s+/g, ' ').trim() || '—'; }
  function breakLongWord(ctx, word, maxWidth) {
    const parts = [];
    let part = '';
    for (const ch of String(word)) {
      const test = part + ch;
      if (ctx.measureText(test).width > maxWidth && part) {
        parts.push(part);
        part = ch;
      } else part = test;
    }
    if (part) parts.push(part);
    return parts;
  }
  function wrapSafe(ctx, text, maxWidth, maxLines = 3) {
    text = clean(text);
    const words = text.split(' ');
    const lines = [];
    let line = '';
    let consumed = 0;
    for (const word of words) {
      if (ctx.measureText(word).width > maxWidth) {
        if (line) { lines.push(line); line = ''; }
        const pieces = breakLongWord(ctx, word, maxWidth);
        for (const p of pieces) {
          if (lines.length < maxLines) { lines.push(p); consumed++; }
          else break;
        }
        if (lines.length >= maxLines) break;
        continue;
      }
      const test = line ? line + ' ' + word : word;
      if (ctx.measureText(test).width <= maxWidth) line = test;
      else {
        if (line) { lines.push(line); consumed++; }
        line = word;
        if (lines.length >= maxLines) break;
      }
    }
    if (line && lines.length < maxLines) { lines.push(line); consumed++; }
    if (!lines.length) lines.push('—');
    const wasClipped = consumed < words.length || lines.join(' ') !== text;
    if (wasClipped) {
      let last = lines[lines.length - 1];
      while (last.length && ctx.measureText(last + '…').width > maxWidth) last = last.slice(0, -1);
      lines[lines.length - 1] = (last || '…') + (last ? '…' : '');
    }
    return lines;
  }
  function drawWrapped(ctx, text, x, firstBaseline, maxWidth, lineHeight, maxLines = 3, align = 'left') {
    const oldAlign = ctx.textAlign;
    ctx.textAlign = align;
    const lines = wrapSafe(ctx, text, maxWidth, maxLines);
    lines.forEach((line, i) => ctx.fillText(line, x, firstBaseline + i * lineHeight));
    ctx.textAlign = oldAlign;
    return lines.length;
  }
  function drawFit(ctx, text, x, y, maxWidth, startPx, minPx = 6, family = FONT, weight = '') {
    const value = clean(text);
    let size = startPx;
    while (size > minPx) {
      ctx.font = `${weight ? weight + ' ' : ''}${size}px ${family}`;
      if (ctx.measureText(value).width <= maxWidth) {
        ctx.fillText(value, x, y);
        return;
      }
      size -= 0.35;
    }
    ctx.font = `${weight ? weight + ' ' : ''}${Math.max(minPx, size)}px ${family}`;
    let out = value;
    while (out.length && ctx.measureText(out + '…').width > maxWidth) out = out.slice(0, -1);
    ctx.fillText((out || '—') + (out ? '…' : ''), x, y);
  }
  function drawField(ctx, label, value, x, y, width, opts = {}) {
    const labelWidth = opts.labelWidth || 21;
    const valueX = x + labelWidth;
    const valueWidth = Math.max(10, width - labelWidth);
    ctx.textAlign = 'left';
    ctx.fillStyle = 'rgb(40,55,80)';
    ctx.font = `${FS(opts.labelSize || 8.1)}px ${FONT}`;
    ctx.fillText(label + ':', x, y);
    ctx.fillStyle = 'rgb(40,55,80)';
    ctx.font = `${FS(opts.valueSize || 8.0)}px ${FONT}`;
    const lines = wrapSafe(ctx, value, valueWidth, opts.maxLines || 1);
    const lineH = opts.lineHeight || FS(5.1);
    lines.forEach((line, i) => ctx.fillText(line, valueX, y + i * lineH));
  }
  function clipBox(ctx, x, y, w, h, drawFn) {
    ctx.save();
    ctx.beginPath(); ctx.rect(x, y, w, h); ctx.clip();
    drawFn();
    ctx.restore();
  }
  function safeCentered(ctx, text, x, y, width, size, minSize = 6, weight = '') {
    ctx.textAlign = 'center';
    drawFit(ctx, text, x, y, width, size, minSize, FONT, weight);
  }

  function newPage() {
    const c = document.createElement('canvas');
    c.width = px(PW); c.height = px(PH);
    const ctx = c.getContext('2d');
    ctx.textBaseline = 'alphabetic';
    ctx.fillStyle = '#ffffff'; ctx.fillRect(0, 0, c.width, c.height);
    ctx.save();
    ctx.globalAlpha = 0.035; ctx.fillStyle = 'rgb(14,165,233)';
    ctx.font = `bold ${FS(42)}px ${FONT}`; ctx.textAlign = 'center';
    ctx.translate(px(PW / 2), px(PH / 2)); ctx.rotate(-28 * Math.PI / 180);
    ctx.fillText('ATLAS EXPRESS SERVICE', 0, 0); ctx.restore();
    ctx.strokeStyle = 'rgb(210,224,240)'; ctx.lineWidth = px(0.35);
    ctx.strokeRect(px(8), px(8), px(PW - 16), px(PH - 16));
    return { c, ctx };
  }

  function drawHeader(ctx, pageLabel) {
    ctx.fillStyle = 'rgb(10,31,61)'; ctx.fillRect(0, 0, px(PW), px(34));
    ctx.fillStyle = 'rgb(14,165,233)'; ctx.fillRect(0, px(34), px(PW), px(2.2));
    fillRR(ctx, px(M), px(8), px(17), px(17), px(3), 'rgb(14,165,233)');
    ctx.fillStyle = '#fff'; ctx.textAlign = 'center'; ctx.font = `bold ${FS(12.4)}px ${FONT}`;
    ctx.fillText('AE', px(M + 8.5), px(19.5));
    ctx.textAlign = 'left'; ctx.fillStyle = '#fff'; ctx.font = `bold ${FS(15)}px ${FONT}`;
    ctx.fillText('ATLAS EXPRESS SERVICE', px(M + 22), px(14.3));
    ctx.font = `${FS(7.2)}px ${FONT}`; ctx.fillStyle = 'rgb(180,210,240)';
    ctx.fillText('✈  ALWAYS AT YOUR SERVICE  ✈', px(M + 22), px(19));
    ctx.fillText('Unit 12 Heron Avenue, Wickford, Essex, SS11 8DL', px(M + 22), px(23.7));
    ctx.fillText('atlasexpressserviceuk@gmail.com  ·  +44 7727 495347', px(M + 22), px(28.4));
    ctx.textAlign = 'right'; ctx.fillStyle = '#fff'; ctx.font = `bold ${FS(11.5)}px ${FONT}`;
    safeCentered(ctx, pageLabel, px(PW - M - 24), px(14.3), px(43), FS(11.5), FS(7), 'bold');
    ctx.textAlign = 'right'; ctx.font = `${FS(6.8)}px ${FONT}`; ctx.fillStyle = 'rgb(180,210,240)';
    safeCentered(ctx, 'Tracking ID: ' + clean(s.id), px(PW - M - 24), px(19), px(43), FS(6.8), FS(5.5));
    safeCentered(ctx, 'Issued: ' + new Date().toLocaleDateString(undefined, {month:'short',day:'numeric',year:'numeric'}), px(PW - M - 24), px(23.7), px(43), FS(6.8), FS(5.5));
    const qrSize = 20, qrX = PW - M - qrSize, qrY = 6;
    fillRR(ctx, px(qrX), px(qrY), px(qrSize), px(qrSize), px(1.5), '#fff');
    if (qrImg) { try { ctx.drawImage(qrImg, px(qrX + 1.3), px(qrY + 1.3), px(qrSize - 2.6), px(qrSize - 2.6)); } catch(e) {} }
    ctx.textAlign = 'center'; ctx.fillStyle = 'rgb(200,222,245)'; ctx.font = `${FS(5.2)}px ${FONT}`;
    ctx.fillText('SCAN TO TRACK', px(qrX + qrSize / 2), px(qrY + qrSize + 4));
  }

  function drawFooter(ctx, pageNum) {
    ctx.strokeStyle = 'rgb(210,224,240)'; ctx.lineWidth = px(0.3);
    ctx.beginPath(); ctx.moveTo(px(M), px(PH - 16)); ctx.lineTo(px(PW - M), px(PH - 16)); ctx.stroke();
    ctx.textAlign = 'center'; ctx.fillStyle = 'rgb(120,135,155)'; ctx.font = `${FS(7)}px ${FONT}`;
    safeCentered(ctx, 'Track online: ' + clean(buildTrackLink(s.id)), px(PW/2), px(PH-11), px(CW), FS(7), FS(5));
    safeCentered(ctx, 'This is an official shipping document issued by ATLAS EXPRESS SERVICE  ·  Page ' + pageNum + ' of 3', px(PW/2), px(PH-6.5), px(CW), FS(7), FS(5));
  }
  function drawSectionTitle(ctx, yy, text) {
    fillRR(ctx, px(M), px(yy), px(CW), px(8.6), px(1.5), 'rgb(10,31,61)');
    ctx.fillStyle='#fff'; ctx.textAlign='left'; ctx.font=`bold ${FS(9.4)}px ${FONT}`;
    ctx.fillText(text, px(M+3.5), px(yy+6.1));
  }
  function drawStar(ctx,cx,cy,r,color){ctx.save();ctx.translate(cx,cy);ctx.beginPath();for(let i=0;i<10;i++){const a=Math.PI/5*i-Math.PI/2,rr=i%2===0?r:r*.42,x=Math.cos(a)*rr,y2=Math.sin(a)*rr;i===0?ctx.moveTo(x,y2):ctx.lineTo(x,y2)}ctx.closePath();ctx.fillStyle=color;ctx.fill();ctx.restore();}
  function drawCircleStamp(ctx,cx,cy,r,mainText,subText,serial,color,angle){
    ctx.save();ctx.translate(cx,cy);ctx.rotate((angle||0)*Math.PI/180);ctx.globalAlpha=.82;ctx.strokeStyle=color;ctx.fillStyle=color;ctx.lineWidth=r*.05;
    ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.stroke();ctx.lineWidth=r*.026;ctx.beginPath();ctx.arc(0,0,r*.83,0,Math.PI*2);ctx.stroke();drawStar(ctx,0,-r*.46,r*.13,color);
    ctx.textAlign='center';ctx.font=`bold ${r*.165}px ${FONT}`;const words=String(mainText).split(' ');
    if(words.length>1&&mainText.length>10){const mid=Math.ceil(words.length/2);safeCentered(ctx,words.slice(0,mid).join(' '),0,-r*.02,r*1.45,r*.165,r*.105,'bold');safeCentered(ctx,words.slice(mid).join(' '),0,r*.17,r*1.45,r*.165,r*.105,'bold');}
    else safeCentered(ctx,mainText,0,r*.06,r*1.55,r*.165,r*.105,'bold');
    safeCentered(ctx,subText,0,r*.42,r*1.55,r*.10,r*.07);safeCentered(ctx,serial,0,r*.6,r*1.55,r*.085,r*.055);ctx.restore();
  }
  function drawSignature(ctx,x,y,w,role,name){
    ctx.textAlign='left';ctx.fillStyle='rgb(10,31,61)';ctx.font=`bold ${FS(7.6)}px ${FONT}`;ctx.fillText(role.toUpperCase(),px(x),px(y));
    clipBox(ctx,px(x),px(y+2),px(w),px(14),()=>{ctx.fillStyle='rgb(30,40,60)';ctx.textAlign='center';drawFit(ctx,name,px(x+w/2),px(y+13),px(w-2),FS(17.5),FS(7.5),SIGFONT);});
    ctx.strokeStyle='rgb(120,135,155)';ctx.lineWidth=px(.28);ctx.beginPath();ctx.moveTo(px(x),px(y+16.5));ctx.lineTo(px(x+w),px(y+16.5));ctx.stroke();ctx.textAlign='center';ctx.fillStyle='rgb(120,135,155)';ctx.font=`${FS(6.6)}px ${FONT}`;ctx.fillText('Authorized Signature',px(x+w/2),px(y+21.5));
  }
  function parseMoney(str){if(str==null)return null;const n=parseFloat(String(str).replace(/[^0-9.\-]/g,''));return isNaN(n)?null:n;}
  function fmtMoney(n){return '£'+n.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2});}

  // PAGE 1 — every form value is confined to its own box/cell.
  const p1=newPage(); drawHeader(p1.ctx,'SHIPPING INVOICE'); let y=44;
  fillRR(p1.ctx,px(M),px(y),px(CW),px(9),px(2),'rgb(14,165,233)');
  safeCentered(p1.ctx,'TRACKING ID: '+clean(s.id),px(PW/2),px(y+6.2),px(CW-8),FS(11.5),FS(7),'bold'); y+=15;

  const col1x=M,col2x=PW/2+2,colW=CW/2-2,infoH=42;
  fillRR(p1.ctx,px(col1x),px(y),px(colW),px(infoH),px(2),'rgb(247,251,255)');strokeRR(p1.ctx,px(col1x),px(y),px(colW),px(infoH),px(2),'rgb(26,63,114)',px(.35));
  p1.ctx.fillStyle='rgb(10,31,61)';p1.ctx.font=`bold ${FS(8.8)}px ${FONT}`;p1.ctx.textAlign='left';p1.ctx.fillText('SENDER DETAILS',px(col1x+4),px(y+6.5));
  drawField(p1.ctx,'Name',s.senderName,px(col1x+4),px(y+13.5),px(colW-8),{maxLines:1});
  drawField(p1.ctx,'Phone',s.senderPhone,px(col1x+4),px(y+19.2),px(colW-8),{maxLines:1});
  drawField(p1.ctx,'Email',s.senderEmail,px(col1x+4),px(y+24.9),px(colW-8),{maxLines:1});
  drawField(p1.ctx,'Origin',s.origin,px(col1x+4),px(y+30.6),px(colW-8),{labelWidth:21,maxLines:2,lineHeight:FS(4.9),valueSize:7.5});
  fillRR(p1.ctx,px(col2x),px(y),px(colW),px(infoH),px(2),'rgb(240,249,255)');strokeRR(p1.ctx,px(col2x),px(y),px(colW),px(infoH),px(2),'rgb(14,165,233)',px(.35));
  p1.ctx.fillStyle='rgb(10,31,61)';p1.ctx.font=`bold ${FS(8.8)}px ${FONT}`;p1.ctx.textAlign='left';p1.ctx.fillText('RECEIVER DETAILS',px(col2x+4),px(y+6.5));
  drawField(p1.ctx,'Name',s.receiverName,px(col2x+4),px(y+13.5),px(colW-8),{maxLines:1});
  drawField(p1.ctx,'Phone',s.receiverPhone,px(col2x+4),px(y+19.2),px(colW-8),{maxLines:1});
  drawField(p1.ctx,'Email',s.receiverEmail,px(col2x+4),px(y+24.9),px(colW-8),{maxLines:1});
  drawField(p1.ctx,'Destination',s.destination,px(col2x+4),px(y+30.6),px(colW-8),{labelWidth:25,maxLines:2,lineHeight:FS(4.9),valueSize:7.5});
  y+=50;

  drawSectionTitle(p1.ctx,y,'SHIPMENT DETAILS');y+=11;
  fillRR(p1.ctx,px(M),px(y),px(CW),px(8),px(1.5),'rgb(186,224,247)');
  p1.ctx.fillStyle='rgb(10,31,61)';p1.ctx.font=`bold ${FS(8)}px ${FONT}`;p1.ctx.textAlign='left';
  const descW=CW*.45,weightW=CW*.16,qtyW=CW*.13,valueW=CW*.26;
  const c2=M+descW,c3=c2+weightW,c4=c3+qtyW;
  p1.ctx.fillText('DESCRIPTION',px(M+3),px(y+5.4));p1.ctx.fillText('WEIGHT',px(c2+2),px(y+5.4));p1.ctx.fillText('QTY',px(c3+2),px(y+5.4));p1.ctx.fillText('DECLARED VALUE',px(c4+2),px(y+5.4));
  y+=9;
  const itemH=17; p1.ctx.fillStyle='#fff';p1.ctx.fillRect(px(M),px(y),px(CW),px(itemH));p1.ctx.strokeStyle='rgb(224,232,242)';p1.ctx.lineWidth=px(.25);p1.ctx.strokeRect(px(M),px(y),px(CW),px(itemH));
  p1.ctx.fillStyle='rgb(20,30,50)';p1.ctx.font=`${FS(8.1)}px ${FONT}`;p1.ctx.textAlign='left';
  clipBox(p1.ctx,px(M+3),px(y+1),px(descW-5),px(itemH-2),()=>drawWrapped(p1.ctx,s.goods,px(M+3),px(y+6),px(descW-5),FS(4.5),3));
  clipBox(p1.ctx,px(c2+2),px(y+1),px(weightW-4),px(itemH-2),()=>drawFit(p1.ctx,s.weight,px(c2+2),px(y+7),px(weightW-4),FS(7.8),FS(5.8)));
  clipBox(p1.ctx,px(c3+2),px(y+1),px(qtyW-4),px(itemH-2),()=>drawFit(p1.ctx,s.quantity||'1',px(c3+2),px(y+7),px(qtyW-4),FS(7.8),FS(5.8)));
  clipBox(p1.ctx,px(c4+2),px(y+1),px(valueW-4),px(itemH-2),()=>drawFit(p1.ctx,s.declaredValue||'N/A',px(c4+2),px(y+7),px(valueW-4),FS(7.8),FS(5.8)));
  y+=itemH+6;

  drawSectionTitle(p1.ctx,y,'TRACKING INFORMATION');y+=11;p1.ctx.fillStyle='rgb(30,45,70)';p1.ctx.textAlign='left';
  drawFit(p1.ctx,'Service Type:  '+clean(s.service||'—'),px(M+2),px(y),px(CW*.50),FS(8.2),FS(5.8));
  drawFit(p1.ctx,'Date Shipped:  '+clean(fmtDateOnly(s.dateShipped)),px(M+2),px(y+7.8),px(CW*.50),FS(8.2),FS(5.8));
  drawFit(p1.ctx,'Expected Delivery:  '+clean(fmtDateOnly(s.eta)),px(M+2),px(y+15.6),px(CW*.50),FS(8.2),FS(5.8));
  drawFit(p1.ctx,'Current Stage:  '+clean(s.status),px(M+2),px(y+23.4),px(CW*.50),FS(8.2),FS(5.8));
  const bx=M+CW*.55,bw=CW*.45;fillRR(p1.ctx,px(bx),px(y-4),px(bw),px(22),px(1.5),'rgb(247,250,253)');strokeRR(p1.ctx,px(bx),px(y-4),px(bw),px(22),px(1.5),'rgb(210,222,238)',px(.3));
  p1.ctx.strokeStyle='rgb(15,25,40)';for(let i=0;i<28;i++){const bw2=Math.sin(i*17)+1.4;p1.ctx.lineWidth=px(.35*bw2);const bxi=bx+3+i*(bw-6)/28;p1.ctx.beginPath();p1.ctx.moveTo(px(bxi),px(y-1));p1.ctx.lineTo(px(bxi),px(y+14));p1.ctx.stroke();}
  safeCentered(p1.ctx,clean(s.id),px(bx+bw/2),px(y+19),px(bw-6),FS(6.2),FS(4.8));y+=33;

  const isDelivered=s.status==='Delivered',isTransit=!isDelivered&&s.status!=='Shipment Created',bannerColor=isDelivered?'rgb(26,163,122)':isTransit?'rgb(10,31,61)':'rgb(74,96,128)';
  fillRR(p1.ctx,px(M),px(y),px(CW),px(11),px(2),bannerColor);safeCentered(p1.ctx,'CURRENT STAGE: '+clean(s.status).toUpperCase(),px(PW/2),px(y+7.4),px(CW-8),FS(9.8),FS(6.2),'bold');y+=20;
  const sigW=(CW-16)/3;drawSignature(p1.ctx,M,y,sigW,'Manager','J. Franklin');drawSignature(p1.ctx,M+sigW+8,y,sigW,'Sender',s.senderName);drawSignature(p1.ctx,M+(sigW+8)*2,y,sigW,'Security','M. Ortega');y+=34;
  const stampY=y+13;drawCircleStamp(p1.ctx,px(M+22),px(stampY),px(15),'LEGAL GOODS','CUSTOMS CLEARED','AE-LG-'+String(s.id).slice(-5),'rgb(24,140,92)',-10);drawCircleStamp(p1.ctx,px(PW/2),px(stampY),px(15),'SHIPPING APPROVAL','ATLAS EXPRESS','AE-SA-'+String(s.id).slice(-5),'rgb(20,90,170)',7);drawCircleStamp(p1.ctx,px(PW-M-22),px(stampY),px(15),'SECURITY CHECKED','VERIFIED','AE-SC-'+String(s.id).slice(-5),'rgb(178,58,58)',-6);drawFooter(p1.ctx,1);

  // PAGE 2 — fixed legal content, still clipped to the printable width.
  const p2=newPage();drawHeader(p2.ctx,'TERMS & CONDITIONS');y=46;p2.ctx.textAlign='left';p2.ctx.fillStyle='rgb(10,31,61)';p2.ctx.font=`bold ${FS(14.5)}px ${FONT}`;p2.ctx.fillText('TERMS & CONDITIONS OF CARRIAGE',px(M),px(y));y+=5;p2.ctx.strokeStyle='rgb(14,165,233)';p2.ctx.lineWidth=px(.6);p2.ctx.beginPath();p2.ctx.moveTo(px(M),px(y));p2.ctx.lineTo(px(M+50),px(y));p2.ctx.stroke();y+=13;
  const terms=[['1. Payment Terms','Full payment of the amount stated on this invoice must be settled prior to pickup or final delivery, unless otherwise agreed in writing by ATLAS EXPRESS SERVICE.'],['2. Customs & Inspection','All goods are subject to inspection, valuation, and customs clearance by relevant authorities in the origin and destination countries. The sender is responsible for ensuring goods are legal for export and import.'],['3. Limitation of Liability','ATLAS EXPRESS SERVICE is not liable for delays, loss, or damage arising from causes beyond its reasonable control, including but not limited to weather, customs holds, strikes, or force majeure events.'],['4. Claims Procedure','Any claim relating to loss, damage, or delay must be submitted in writing within seven (7) calendar days of the scheduled or actual delivery date. Claims received after this period may not be honored.'],['5. Accuracy of Information','The sender warrants that all information provided, including item description, weight, declared value, and recipient details, is true, complete, and accurate. False declarations may result in delays, fines, or seizure of goods.'],['6. Prohibited Items','Shipments must not contain items prohibited by international shipping law or the customs regulations of the destination country. ATLAS EXPRESS SERVICE reserves the right to inspect and refuse any shipment.'],['7. Risk of Loss','Risk of loss or damage passes to the receiver upon confirmed delivery at the destination address stated on this invoice.'],['8. Governing Law','This agreement is governed by the shipping regulations applicable at the port of origin, without regard to conflict-of-law principles.']];
  terms.forEach(([head,body])=>{p2.ctx.fillStyle='rgb(10,31,61)';p2.ctx.font=`bold ${FS(9.2)}px ${FONT}`;p2.ctx.textAlign='left';p2.ctx.fillText(head,px(M),px(y));y+=6.6;p2.ctx.fillStyle='rgb(55,65,85)';p2.ctx.font=`${FS(8.2)}px ${FONT}`;const lines=wrapSafe(p2.ctx,body,px(CW),7);lines.forEach(line=>{p2.ctx.fillText(line,px(M),px(y));y+=6});y+=4;});
  y+=8;p2.ctx.fillStyle='rgb(30,40,60)';p2.ctx.font=`italic ${FS(8)}px ${FONT}`;drawFit(p2.ctx,'By accepting this shipment, the sender and receiver acknowledge and agree to the terms above.',px(M),px(y),px(CW),FS(8),FS(5.5),FONT,'italic');drawCircleStamp(p2.ctx,px(PW-M-26),px(PH-46),px(18),'APPROVED','TERMS ACCEPTED','AE-TC-'+String(s.id).slice(-5),'rgb(24,140,92)',-12);drawFooter(p2.ctx,2);

  // PAGE 3 — all payment/user values are fitted to their columns.
  const p3=newPage();drawHeader(p3.ctx,'TIMELINE & PAYMENT');y=46;drawSectionTitle(p3.ctx,y,'SHIPPING TIMELINE');y+=11;
  const curIdx=STAGES.indexOf(s.status),stageTimes={};(s.history||[]).forEach(h=>{if(STAGES.includes(h.status))stageTimes[h.status]=h.time});if(!stageTimes['Shipment Created'])stageTimes['Shipment Created']=s.created;
  STAGES.forEach((stage,i)=>{const done=curIdx>=0&&i<=curIdx;p3.ctx.fillStyle=i%2===0?'rgb(247,251,255)':'#fff';p3.ctx.fillRect(px(M),px(y),px(CW),px(9.6));p3.ctx.fillStyle=done?'rgb(14,165,233)':'rgb(175,185,200)';p3.ctx.font=`${done?'bold ':''}${FS(9)}px ${FONT}`;p3.ctx.textAlign='left';p3.ctx.fillText((done?'✓  ':'○  ')+stage,px(M+3),px(y+6.6));const t=stageTimes[stage];p3.ctx.textAlign='right';p3.ctx.fillStyle='rgb(120,130,150)';p3.ctx.font=`${FS(7.4)}px ${FONT}`;drawFit(p3.ctx,t?fmtDate(t):(done?'—':'Pending'),px(PW-M-3),px(y+6.4),px(CW*.45),FS(7.4),FS(5.2),FONT);y+=9.6;});y+=10;
  drawSectionTitle(p3.ctx,y,'PAYMENT SUMMARY');y+=11;const declared=parseMoney(s.declaredValue),total=parseMoney(s.amountToPay);let shipFee=null;if(declared!=null&&total!=null&&total>=declared)shipFee=total-declared;
  const rows=[['Subtotal (Declared Value)',declared!=null?fmtMoney(declared):(s.declaredValue||'N/A')],['Shipping & Handling Fee',shipFee!=null?fmtMoney(shipFee):'—']];rows.forEach((r,i)=>{p3.ctx.fillStyle=i%2===0?'rgb(247,251,255)':'#fff';p3.ctx.fillRect(px(M),px(y),px(CW),px(9.2));p3.ctx.fillStyle='rgb(70,85,105)';p3.ctx.font=`${FS(8.8)}px ${FONT}`;p3.ctx.textAlign='left';p3.ctx.fillText(r[0],px(M+3),px(y+6.2));p3.ctx.fillStyle='rgb(10,31,61)';p3.ctx.font=`bold ${FS(8.8)}px ${FONT}`;drawFit(p3.ctx,r[1],px(PW-M-3),px(y+6.2),px(CW*.38),FS(8.8),FS(5.5),FONT,'bold');p3.ctx.textAlign='left';y+=9.2;});
  fillRR(p3.ctx,px(M),px(y+2),px(CW),px(12.4),px(2),'rgb(10,31,61)');p3.ctx.fillStyle='#fff';p3.ctx.textAlign='left';p3.ctx.font=`bold ${FS(9.6)}px ${FONT}`;p3.ctx.fillText('TOTAL AMOUNT TO BE PAID',px(M+4),px(y+10.1));drawFit(p3.ctx,total!=null?fmtMoney(total):(s.amountToPay||'N/A'),px(PW-M-4),px(y+10.1),px(CW*.38),FS(12.6),FS(6),FONT,'bold');y+=30;
  drawSignature(p3.ctx,M,y,70,'Manager','J. Franklin');drawCircleStamp(p3.ctx,px(PW-M-26),px(y-6),px(16),'SECURITY CHECKED','FINAL VERIFICATION','AE-FV-'+String(s.id).slice(-5),'rgb(178,58,58)',9);drawFooter(p3.ctx,3);

  const {jsPDF}=window.jspdf;const doc=new jsPDF({unit:'mm',format:'a4',orientation:'portrait'});doc.addImage(p1.c.toDataURL('image/jpeg',.93),'JPEG',0,0,PW,PH);doc.addPage();doc.addImage(p2.c.toDataURL('image/jpeg',.93),'JPEG',0,0,PW,PH);doc.addPage();doc.addImage(p3.c.toDataURL('image/jpeg',.93),'JPEG',0,0,PW,PH);doc.save('AtlasExpress-Invoice-'+s.id+'.pdf');showToast('3-page invoice PDF downloaded ✓','success');
}

function esc(str) {
  const d = document.createElement('div');
  d.textContent = str || '';
  return d.innerHTML;
}
function parseMoney(str) {
  if (str == null) return null;
  const n = parseFloat(String(str).replace(/[^0-9.\-]/g, ''));
  return isNaN(n) ? null : n;
}
function fmtMoney(n) {
  if (n == null || isNaN(n)) return 'N/A';
  return '£' + n.toLocaleString(undefined, { minimumFractionDigits: 2, maximumFractionDigits: 2 });
}
function setText(id, val) {
  const el = document.getElementById(id);
  if (el) el.textContent = val || '—';
}
function statusClass(s) {
  if (s === 'Delivered') return 'delivered';
  if (s === 'Customs') return 'customs';
  if (s === 'Shipment Created') return 'pending';
  return 'transit';
}
function fmtDate(iso) {
  if (!iso) return '—';
  return new Date(iso).toLocaleString(undefined, { month:'short', day:'numeric', year:'numeric', hour:'2-digit', minute:'2-digit' });
}
function fmtDateOnly(iso) {
  if (!iso) return '—';
  const d = new Date(iso + (iso.includes('T') ? '' : 'T00:00:00'));
  return d.toLocaleDateString(undefined, { month:'long', day:'numeric', year:'numeric' });
}
</script>
</body>
</html>
